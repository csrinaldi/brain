{"id":"rec-f1fb2fe139854d32","ts":"2026-08-02T12:11:34Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"session_summary","project":"brain","content":"**Session summary: brain — phase-0 epic re-sync (#402, PR #403)**\n\n## Goal\nPhase-0 bookkeeping of epic #313, after a full-scope architecture audit of `main` @ `5ef85df`: re-sync the docs that drifted from the shipped tree, clear the standing drift-guard, and close tickets already satisfied in code. Ref: issue #402, PR #403.\n\n## What was done\n- **`docs/KNOWN-LIMITATIONS.md` re-synced.** It still claimed the reviewer flow-guarantees were inert (#317) and that the release gate ran after the tag (#210) — both shipped. Also corrected GitLab parity (all 8 MR gates blocking since Q5 Phase 5; the residual is rung-2/rung-3, #130) and replaced the \"un-ticketed\" M4 prose with the new ticket refs.\n- **`AGENTS.md` regenerated** from its 5 SOURCE_DOCS via the antigravity backend — the M3 merge changed `reviewer-protocol.md`, leaving the drift-guard red. Suite 2263/2264 → 2264/2264.\n- Epic #313 body updated with a dated status block; #369 and #263 closed as satisfied-in-code; M4 hard gate decomposed into #396-#401.\n\n## BUG FOUND — the engram transport drops `issue`, and it blocks #368 / T2.1\nWhile capturing this very record scoped to issue #402, the standing REQ-C4-1 contract test went red: the record failed round-trip id-equality. Root cause, traced through the seam:\n\n- `buildRecord` includes `issue` in the hashInput (per memory-format.md:67), so a scoped record hashes differently.\n- `importRecord` (record -> engram observation) has **no home for `issue`** — the observation shape is `{type, project, title, content, created_at, scope}`.\n- `exportObservation` therefore cannot restore it, so the recomputed id differs from the stored id (`rec-4e5cce32ee0b4047` vs the stored `rec-c0d159bba82a6a95`).\n\nConsequence: **any record carrying `issue` fails REQ-C4-1 today.** This is the mechanical reason #368 sits at 0% adoption — populating the field is not merely unadopted, it is currently *impossible* without breaking the round-trip contract. Since T2.1 memory-gate promotion depends on issue-scoped records, this seam is on the critical path for that promotion. The record was therefore written WITHOUT `issue`, matching every other record in the store, and the defect filed separately.\n\nThis is a seam-blindness defect of exactly the class M10 (#335) exists to close: green suite, contract silently unsatisfiable in the one direction nobody exercised.\n\n## Findings worth carrying forward\n- **The epic body drifted from its own tickets.** A roadmap pass derived from it proposed #210 as \"1.1 first item\" — #210 is closed and shipped. A source-of-truth doc needs a re-sync ritual or it misdirects the next agent.\n- **M3 core is merged but the milestone exit is not met.** `5ef85df` wired the refuter (via `lib/causal-admission.mjs`) and tier-activated `/2`, but there are still no inline per-line comments — the literal exit criterion. Residuals: #381 (findings do not round-trip: render emits a YAML list, parse reads a JSON scalar), #382 (self-review fail-open in code, masked by config), `follow_ups[]` wired but unreachable, `/2` undogfoodable while brain declares `tier: lite`.\n- **M4 was the biggest tracking gap**: the epic hard gate lived only as prose. Now #396 (rollback/atomic), #397 (clobber asymmetry), #398 (downgrade ratchet), #399 (corrupt-JSON lockout), #400 (npx brain init), #401 (danger-path e2e — makes the gate checkable).\n- **Recurring defect class**: \"one rule, two implementations\" — #315, #316, #340. Worth batching as one initiative with drift-guards.\n\n## Decisions\n- One PR for both files: same atomic cause (the M3 merge), 130 lines, splitting would double the gate ritual for docs-only changes.\n- Record written without `issue` rather than shipping a store that fails its own contract test.\n- No `status:approved` applied by the agent — that is the human keystroke per #124 doctrine.","source":"plainfiles save on vm"}
{"id":"rec-085ff36f97e13a7e","ts":"2026-08-02T14:38:09Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"session_summary","project":"brain","content":"**Session summary: brain — M3 post-merge defect cleanup (#381, #382) + roadmap re-sync**\n\n## Goal\nContinue Phase 1 after the M3 core merge (`5ef85df`, #394): fix the two known reviewer defects, close the M3 residual tracking gap, and re-sync epic #313 so the roadmap is derivable from tickets rather than prose.\n\n## What shipped\n- **#381 → PR #406 (`c881a04`).** `findings` and `follow_ups` now survive the `renderVerdict` → `parseVerdict` round trip. Root cause: `renderVerdict` emits a YAML block list, `parseVerdict` read it with `scalar()`, whose `^findings:[ \\t]*(.+)$` requires same-line content. Only `findings: []` matched. `follow_ups` was rendered by M3 but never parsed by anything — write-only since it shipped.\n- **#382 → PR #407 (`e67dff8`).** Self-review abstention now fails closed at boot when `reviewer.handle` is empty. The same missing handle also made the anti-loop guard inert (it compares `lastVerdict.author` against that handle), so one fix restored two guards. The fail-open was deliberate, not an oversight — a strict guard would have abstained from every review while the handle was unresolvable — but that premise expired with #367.\n\n## Tickets filed (M3 residuals — the milestone exit is NOT met)\n- **#409** — `regulated`-tier e2e. Recommended FIRST: it is the harness the other two need to prove anything.\n- **#405** — inline per-line `comments[]`, GitHub+GitLab parity. Needs an ADR-0020 amendment and a Tier-2 promotion of `vcs-contract.md` before any code: no contract for it exists yet.\n- **#408** — no evaluator emits `pre-existing`/`base-only`; `follow_ups[]` is unreachable and the refuter never fires.\n- **#404** — engram transport drops `issue`; blocks #368 → T2.1 promotion.\n- **#410** — this memory materialization.\n\n## Epic #313 re-synced\nCritical path **inverted deliberately**: M4 (#396–#401) is now the highest-priority track, with M3 closure in parallel. Rationale recorded in the body: M4 is the hard gate blocking external adopters *today*; M3 residuals block reviewer *completeness*. The two touch disjoint surfaces, so nothing serialises them. Added a cross-cutting section for #404 (it belongs to no milestone yet blocks #368 → T2.1) and a standing rule for future agents: work described only in prose in that body is a bug — file an issue for it.\n\n## Process notes that cost time\n- Worktree-per-issue is the repo's workflow (`AGENTS.md`, load-bearing). `brain:ticket:start --worktree` fails in Claude-Code-on-the-web (`VCS token not found in .env` — the container has the GitHub MCP server, not a `gh` token); reproduce the branch convention manually from `deriveBranchType` + the 40-char slug rule.\n- Worktrees do not inherit `node_modules`; symlink it or nothing runs.\n- A combined branch being green does not prove each split half is green — run the full suite per worktree. Baseline on `e67dff8` is 2268 tests.\n- Commit signing is impossible in this container (0-byte `commit_signing_key.pub`, no private key, no `ssh-keygen`); merged history already carries unsigned Claude commits.\n- Deleting remote branches is blocked by the git proxy; `fix/issue-381-382-reviewer-roundtrip` is orphaned and needs deleting via the GitHub UI."}
{"id":"rec-2330fa03af44bf5c","ts":"2026-08-02T14:38:09Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"architecture","project":"brain","content":"**Defect class: render/parse asymmetry hidden by testing only the empty encoding (brain, #381)**\n\n`renderVerdict` serialised `findings` as a YAML block list; `parseVerdict` deserialised it with a scalar regex requiring content on the same line. Every non-empty findings list was silently dropped on the round trip.\n\n**The generalisable lesson is why the full suite stayed green.** The only encoding that round-tripped — `findings: []` — was the only encoding under test. Coverage over a serializer/parser pair proves nothing unless the tests exercise the *non-degenerate* shape.\n\nTwo practices adopted in the fix, worth repeating for any wire-format work in this repo:\n1. Tests drive the real `buildVerdict` → `renderVerdict` pair and parse the actual output. Hand-written fixture strings encode the author's belief about the format, which is exactly the thing under doubt.\n2. New tests were run against the OLD parser to confirm they fail (3 of 4 did). The 4th — the empty case — passes against both and was kept deliberately, as an in-repo record of the blind spot that hid the defect.\n\nRelated: this is the same \"green in test, inert in production\" class that M10 (#335) exists to close. It was found by reading the code, not by a failing test, which is itself evidence for #409 — brain runs the `lite` tier, so the whole `/2` path has no production exercise."}
{"id":"rec-75c03284bfd338ff","ts":"2026-08-02T14:38:09Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"architecture","project":"brain","content":"**M3 shipped consumers without producers — three wired-but-unreachable paths (brain)**\n\nAfter the M3 core merge, three code paths exist, are correct, and can never execute in production:\n\n1. **`follow_ups[]`** — `verdict.mjs:51` routes a `causal_disposition` of `pre-existing`/`base-only` into it, `schema-v2.mjs` admits both values, `reviewer-protocol.md:216,222` documents them. **No evaluator emits either.** `causal-admission.mjs:23` states why, correctly: none of the current checks compare against base closely enough to claim it honestly, and \"inventing the claim would be worse than omitting it\". The gap is that no check which *can* make the claim was ever built. Tracked in #408.\n2. **The refuter** (`evaluators/refuter.mjs`, #284) — runs only when a blocker carries `evidence_class: 'inferential'`. No evaluator produces inferential findings, so it is a documented no-op, deliberately wired so a future producer needs no second integration point. Same ticket, #408.\n3. **The entire `/2` tier** — activation routes `lite`/`standard` → `/1` and `regulated` → `/2`, and brain declares `lite` (ADR-0026, #358). Everything M3 built is covered by unit tests and by nothing else. Tracked in #409.\n\n**Consequence to carry forward:** #381 made `follow_ups` *readable*, not *populated* — it moved from broken to empty, not to working. Do not read a green suite or a merged PR as evidence that this feature works end to end.\n\n**Empirical evidence for #404, found while inspecting the store:** zero records across `2026-06.jsonl`, `2026-07.jsonl` and `2026-08.jsonl` carry an `issue` field. That is the \"~0% adoption\" #368 describes, and the cause is the transport dropping the field, not authors omitting it. Note `computeRecordId` DOES hash `issue` when present, so the format supports it — only the transport does not."}
{"id":"rec-1ccebc390604596a","ts":"2026-08-02T19:34:34Z","actor":"claude/issue-396-upgrade-rollback","actorKind":"agent","type":"session_summary","project":"brain","content":"**Session summary: brain — #396 slice 1, restore-point rollback for brain:upgrade**\n\n## Goal\nPick up the brain 1.1 line and advance M4 — the epic #313 hard gate that blocks external\nadopters from upgrading safely. Owner chose M4 over M3 closure, then chose to deliver #396\nas a two-slice chain.\n\n## Accomplished\n- **#396 slice 1 shipped to review** — PR #412 (draft), chain `main ← feature/issue-396-rollback\n  ← PR #412 ← PR#2`. `copyManaged` now snapshots every path it may write before the first\n  write and restores those bytes on any throw; an incomplete rollback KEEPS its snapshot and\n  reports where. Symlinked managed paths are refused up front. Suite 2268 → 2282.\n- **Full SDD change dir written** at `openspec/changes/issue-396-upgrade-rollback/` —\n  proposal, spec (REQ-396-1..9 with scenarios and negative controls), design, tasks, plus\n  `brain-drafts/adr-0027-upgrade-rollback-is-restorable-not-atomic.md`.\n- **Three follow-up issues filed**: #413, #414, #415. Epic #313 re-synced with a status block,\n  a rewritten M4, and an explicit priority table for the next agent.\n\n## Discoveries\n1. **#396's stated exit criterion is unachievable as written.** `rename(2)` is atomic for ONE\n   path; the managed payload spans four disjoint locations (`brain/**`, `.github/**`,\n   `.gemini/**`, root files), so no single directory swap can commit it. Multi-file atomicity\n   needs a journal. Restated as *restorability*, with each residual gap named — ADR-0027 draft,\n   unsigned.\n2. **A signal handler cannot interrupt a synchronous fs loop.** Measured: SIGINT at 40ms into a\n   935ms sync copy loop → the loop ran to completion (i=60000) and the handler fired after;\n   the same loop with a `setImmediate` yield aborted at i=2251. So registering a handler is not\n   a mid-write abort — it suppresses the default terminate action, which is the only thing that\n   genuinely half-applies a tree. The real managed write is 366 files in ~23ms.\n3. **`spawnSync` returns `{status: null, signal}` when the child dies by signal.** A first\n   revision claimed, as measured, that a Ctrl-C during `npm install` was caught by the deferred\n   interrupt guard. It was not: `if (r.status !== 0)` fires first, reporting the interrupt as\n   \"install failed — check repo access\" and collapsing exit 130 into 1. Registering the handler\n   is what kept the parent alive to reach that branch, so the slice introduced that regression.\n   Fixed by testing `r.signal` before `r.status`. Recorded rather than rewritten out of history.\n4. **`existsSync` follows symlinks.** A dangling symlink at a managed path therefore read as\n   absent, was recorded as created-by-this-run, and was DELETED on rollback — while\n   `copyFileSync` wrote through the link to a target outside the repo, and the rollback reported\n   success. The feature corrupting what it exists to protect. Presence must be judged with\n   `lstat`.\n5. **A `finally` is the wrong place to discard a restore point.** It runs after `catch`, so it\n   deleted the snapshot on the incomplete-rollback path — the one branch where the snapshot is\n   the only surviving copy of the consumer's bytes, and the same branch whose message tells the\n   operator to go inspect them. Only a complete rollback may discard.\n6. **Annotating a caught error can throw.** Modules are strict mode, so\n   `err.rollbackIncomplete = …` raises a TypeError on a string, frozen Error or null — replacing\n   the real failure AND dropping the dirty-tree signal, which made the CLI report a clean\n   rollback over a dirty tree. `specialMerge` is caller-supplied on an exported API, so this\n   needs no brain-side bug to reach.\n7. **A test suite can be green and still miss the dominant shape.** The first test round always\n   left exactly ONE entry in `toCopy` — the one that throws — so plain-copy rollback was never\n   exercised, despite 364 of 366 real managed files being plain copies. Every rolled-back write\n   in the suite came from a merge fn. Fixed by sorting the write order and adding REQ-S6-10.\n8. **`brain:review` at `tier: lite` resolves to `brain-review/1`**, which has no\n   findings-producing evaluator: `findings: []` means \"nobody looked\", not \"the code is clean\".\n   `/2` — the half that wires causal-admission and the refuter — is `regulated`-only. This is\n   epic #313's #409 observed live, not a defect.\n9. **The reviewer identity is declared, never proven.** `identity.mjs` returns\n   `reviewerConfig.handle` and nothing cross-checks the token against it, so the §10 self-review\n   abstention and the anti-loop lock are forgeable. Filed as #413; sibling of #382.\n10. **`actor-check`'s `lite` evidence is a distinct ACT, not a state.** The approved-label event\n    must be strictly later than the PR's head commit, so every push invalidates the previous\n    approval. Working as designed — Q5's fix for the stale-green class — but it will read as a\n    broken CI to anyone who has not seen it.\n\n## Corrections made to my own earlier claims\n- Reported `TIER_PARAMS.lite.diffBudget` and `.artefacts` as unconsumed \"one rule, two\n  implementations\" divergences. **Both were wrong**: `diffBudget` is read by `brain-check.mjs`,\n  `brain-audit.mjs` and `merge-walk.mjs`; `artefacts` by `phase-order-check.mjs`. `check-refs`\n  demanding all four artefacts is a deliberately tier-independent STRUCTURAL check. The wrong\n  conclusion came from a mutilated grep (`rg -r` consumed the pattern as a replacement string).\n\n## Next steps\n- **#396 slice 2 — the on-disk journal.** The M4 hard gate stays CLOSED until it lands. It must\n  INVERT `createRestorePoint`'s entry-time clearing of a pre-existing snapshot: once recovery\n  exists, a leftover snapshot is evidence to replay, not debris. Flagged inline.\n- Human: re-apply `status:approved` on #396 after the last push; ratify or reject ADR-0027;\n  Tier-2 promotions for #397, #405 and #414.\n- Then #397/#398/#399, #401, #413, and M3 closure (#409 → #405 → #408).\n\n## Relevant files\n- `brain/scripts/lib/installer.mjs` — `createRestorePoint`, `annotateRollback`, guarded write loop\n- `brain/scripts/lib/installer.test.mjs` — REQ-S6-1..14\n- `brain/scripts/brain-upgrade.mjs` — signal-before-status, rollback reporting, interrupt handling\n- `docs/KNOWN-LIMITATIONS.md` — partial fix with all four residual gaps enumerated\n- `openspec/changes/issue-396-upgrade-rollback/` — proposal, spec, design, tasks, ADR-0027 draft","source":"plainfiles save on vm"}
{"id":"rec-89a522b5330c0ea4","ts":"2026-08-04T09:43:23Z","actor":"claude/m4-upgrade-safety","actorKind":"agent","type":"session_summary","project":"brain","content":"**Session summary: brain — M4 upgrade safety (#396, #399, #398 closed; #397 designed and handed off)**\n\n## Goal\nAdvance M4 — epic #313's HARD GATE, which blocks external adopters from upgrading\nsafely. Closed #396, #399 and #398; designed #397 and handed it off.\n\n## Accomplished\n- **#396 CLOSED** — restore point + on-disk journal + explicit `--recover` + an exclusive\n  lock. ADR-0027 signed. Took EIGHT adversarial review rounds.\n- **#399 CLOSED** — merge pre-flight + `--skip-merge <path>`; the corrupt-JSON lockout has\n  an escape.\n- **#398 CLOSED** — downgrade guard before the install, naming the config keys left ahead.\n- **#397 DESIGNED, ratified, NOT implemented** — handed to a fresh agent.\n- Spun off and closed by the maintainer: **#413** (reviewer identity now verified against\n  the token) and **#418** (lite distinct-act re-arms only on FOREIGN commits — ADR-0026\n  Amendment 1). Still open: **#414** (Tier-2), **#415** (cosmetic).\n\n## Discoveries that cost the most to find\n1. **A green suite proves nothing about a CLI it never runs.** Twice on #396 a fully green\n   suite hid a broken product: once the feature did nothing (the `acquireLock` call site\n   was deleted as collateral of a scripted edit), once `brain:upgrade` failed 100% of the\n   time (the run read its own live lock as a competitor and refused itself). Both passed\n   ~2290 tests. The fix was REQ-J-9 (the lock is HELD while it works) + REQ-J-10 (the\n   upgrade completes and the bytes change). **Neither is sufficient alone.**\n2. **A cleanup that judges locally will eventually delete something another site owns.**\n   Four consecutive rounds produced that same shape, each introduced by the previous fix:\n   a `finally` discarding the snapshot; a retry clearing the preserved copy; a catch\n   destroying the evidence a refusal pointed at; `breakStaleLock` deleting a LIVE run's\n   mutex. Resolved structurally with `inspectRestorePoint()` — ONE reader, ONE verdict\n   (clean/debris/interrupted/corrupt/live-run) — and an ALLOW-list at the one remaining\n   cleanup site.\n3. **Signal handlers cannot interrupt a synchronous fs loop.** Measured: SIGINT at 40ms\n   into a 935ms sync copy ran to completion, handler after. So registering a handler is\n   not a mid-write abort — it stops the default action from killing the process, which is\n   the only thing that half-applies a tree. Corollary, also measured: a run wedged on a\n   hung managed path can then only be ended with SIGKILL.\n4. **\"Could not read it\" is not \"nobody owns it\".** Widening lock reclaim to unowned locks\n   accidentally widened it to UNREADABLE ones (EACCES/EIO/EMFILE/ESTALE), inverting the\n   stated property from \"it strands, it never permits\" to permitting.\n5. **schemaVersion has TWO writers with DIFFERENT meanings** — `ensureBrainConfig`\n   (env:init) writes the newest MIGRATION version, `migrateConfig` writes the installed\n   PACKAGE version, and migrations top out below the package. A downgrade guard trusting\n   that field alone missed the six most recent of sixteen tags — the fresh-adopter\n   population the gate exists for. Compare against the highest thing actually TRUE.\n6. **AGENTS.md is GENERATED and its inputs straddle the ownership line.** `brain/HOME.md`\n   matches neither `managed` nor `local`, so the consumer owns it. Plain-copying AGENTS.md\n   therefore hands every consumer BRAIN's AGENTS.md, compiled from BRAIN's HOME.md — a file\n   describing the wrong repository, in every consumer, from the first upgrade. This is the\n   finding #397 does not contain and it reshaped the design.\n7. **The collision check conflates two facts.** dest-vs-INCOMING answers \"do these differ\",\n   not \"did the consumer edit this\". The missing third point is free: before the install,\n   `node_modules/brain/<path>` is what brain shipped LAST time.\n\n## Corrections I had to make to my own claims\n- I wrote that #399 \"strengthened\" the #180 clobber guard. It WEAKENED it: I moved the\n  assertion from the call site to the declaration, and mutating `specialMerge: mergeMap`\n  to `{}` reproduced #180 with the whole suite green. Fixed with a behavioural test.\n- I described reading the OUTGOING migration list as a tradeoff to apologise for. It is the\n  only thing that works — a target's own list can never contain a migration above itself.\n- Two \"one rule, two implementations\" divergences I reported (`diffBudget`, `artefacts`)\n  were FALSE. Both params are consumed. The wrong conclusion came from `rg -rn`, where\n  `-r` silently consumed the pattern as a replacement string.\n\n## Method that worked, and should be kept\nMeasure the real behaviour before designing — three of the four M4 tickets had premises\nthat had already shifted. Write the test FIRST and prove it red. Drive the real CLI, not\nthe library API. Run one adversarial review per slice: the two I nearly skipped as \"small\nand safe\" each found a real hole in the target population.\n\n## Next steps\n- **#397 implementation** — classification RATIFIED 04/08/2026, phases 2-4 unblocked,\n  handed to a fresh agent. Tier-2 signature already obtained; do not re-litigate the table.\n- **#401** — the danger-path e2e. **This is what makes the hard gate checkable**; three of\n  its four paths now have real behaviour to assert, the fourth arrives with #397.\n- **#400** — `npx brain init`, independent.\n- **THE HARD GATE IS STILL CLOSED.** It opens when #401 is green, not when the fixes land.\n\n## Relevant files\n- brain/scripts/lib/installer.mjs — restore point, journal, recovery, lock, pre-flight, downgrade helpers\n- brain/scripts/brain-upgrade.mjs — the CLI surface and every refusal path\n- brain/scripts/lib/installer-journal.integration.test.mjs — REQ-J-*, REQ-M-*, REQ-D-* (all drive the real CLI)\n- brain/project/decisions/adr-0027-upgrade-rollback-is-restorable-not-atomic.md\n- openspec/changes/issue-397-clobber-asymmetry/ — the ratified design awaiting implementation","source":"plainfiles save on vm"}
{"id":"rec-1b56da533b9b2c2d","ts":"2026-08-04T11:19:11Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"session_summary","project":"brain","content":"**Session summary: brain — phase-0 epic re-sync (#402, PR #403)**\n\n## Goal\nPhase-0 bookkeeping of epic #313, after a full-scope architecture audit of `main` @ `5ef85df`: re-sync the docs that drifted from the shipped tree, clear the standing drift-guard, and close tickets already satisfied in code. Ref: issue #402, PR #403.\n\n## What was done\n- **`docs/KNOWN-LIMITATIONS.md` re-synced.** It still claimed the reviewer flow-guarantees were inert (#317) and that the release gate ran after the tag (#210) — both shipped. Also corrected GitLab parity (all 8 MR gates blocking since Q5 Phase 5; the residual is rung-2/rung-3, #130) and replaced the \"un-ticketed\" M4 prose with the new ticket refs.\n- **`AGENTS.md` regenerated** from its 5 SOURCE_DOCS via the antigravity backend — the M3 merge changed `reviewer-protocol.md`, leaving the drift-guard red. Suite 2263/2264 → 2264/2264.\n- Epic #313 body updated with a dated status block; #369 and #263 closed as satisfied-in-code; M4 hard gate decomposed into #396-#401.\n\n## BUG FOUND — the engram transport drops `issue`, and it blocks #368 / T2.1\nWhile capturing this very record scoped to issue #402, the standing REQ-C4-1 contract test went red: the record failed round-trip id-equality. Root cause, traced through the seam:\n\n- `buildRecord` includes `issue` in the hashInput (per memory-format.md:67), so a scoped record hashes differently.\n- `importRecord` (record -> engram observation) has **no home for `issue`** — the observation shape is `{type, project, title, content, created_at, scope}`.\n- `exportObservation` therefore cannot restore it, so the recomputed id differs from the stored id (`rec-4e5cce32ee0b4047` vs the stored `rec-c0d159bba82a6a95`).\n\nConsequence: **any record carrying `issue` fails REQ-C4-1 today.** This is the mechanical reason #368 sits at 0% adoption — populating the field is not merely unadopted, it is currently *impossible* without breaking the round-trip contract. Since T2.1 memory-gate promotion depends on issue-scoped records, this seam is on the critical path for that promotion. The record was therefore written WITHOUT `issue`, matching every other record in the store, and the defect filed separately.\n\nThis is a seam-blindness defect of exactly the class M10 (#335) exists to close: green suite, contract silently unsatisfiable in the one direction nobody exercised.\n\n## Findings worth carrying forward\n- **The epic body drifted from its own tickets.** A roadmap pass derived from it proposed #210 as \"1.1 first item\" — #210 is closed and shipped. A source-of-truth doc needs a re-sync ritual or it misdirects the next agent.\n- **M3 core is merged but the milestone exit is not met.** `5ef85df` wired the refuter (via `lib/causal-admission.mjs`) and tier-activated `/2`, but there are still no inline per-line comments — the literal exit criterion. Residuals: #381 (findings do not round-trip: render emits a YAML list, parse reads a JSON scalar), #382 (self-review fail-open in code, masked by config), `follow_ups[]` wired but unreachable, `/2` undogfoodable while brain declares `tier: lite`.\n- **M4 was the biggest tracking gap**: the epic hard gate lived only as prose. Now #396 (rollback/atomic), #397 (clobber asymmetry), #398 (downgrade ratchet), #399 (corrupt-JSON lockout), #400 (npx brain init), #401 (danger-path e2e — makes the gate checkable).\n- **Recurring defect class**: \"one rule, two implementations\" — #315, #316, #340. Worth batching as one initiative with drift-guards.\n\n## Decisions\n- One PR for both files: same atomic cause (the M3 merge), 130 lines, splitting would double the gate ritual for docs-only changes.\n- Record written without `issue` rather than shipping a store that fails its own contract test.\n- No `status:approved` applied by the agent — that is the human keystroke per #124 doctrine.","source":"plainfiles save on vm"}
{"id":"rec-c438887dbd2b02ee","ts":"2026-08-04T11:19:12Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"session_summary","project":"brain","content":"**Session summary: brain — M3 post-merge defect cleanup (#381, #382) + roadmap re-sync**\n\n## Goal\nContinue Phase 1 after the M3 core merge (`5ef85df`, #394): fix the two known reviewer defects, close the M3 residual tracking gap, and re-sync epic #313 so the roadmap is derivable from tickets rather than prose.\n\n## What shipped\n- **#381 → PR #406 (`c881a04`).** `findings` and `follow_ups` now survive the `renderVerdict` → `parseVerdict` round trip. Root cause: `renderVerdict` emits a YAML block list, `parseVerdict` read it with `scalar()`, whose `^findings:[ \\t]*(.+)$` requires same-line content. Only `findings: []` matched. `follow_ups` was rendered by M3 but never parsed by anything — write-only since it shipped.\n- **#382 → PR #407 (`e67dff8`).** Self-review abstention now fails closed at boot when `reviewer.handle` is empty. The same missing handle also made the anti-loop guard inert (it compares `lastVerdict.author` against that handle), so one fix restored two guards. The fail-open was deliberate, not an oversight — a strict guard would have abstained from every review while the handle was unresolvable — but that premise expired with #367.\n\n## Tickets filed (M3 residuals — the milestone exit is NOT met)\n- **#409** — `regulated`-tier e2e. Recommended FIRST: it is the harness the other two need to prove anything.\n- **#405** — inline per-line `comments[]`, GitHub+GitLab parity. Needs an ADR-0020 amendment and a Tier-2 promotion of `vcs-contract.md` before any code: no contract for it exists yet.\n- **#408** — no evaluator emits `pre-existing`/`base-only`; `follow_ups[]` is unreachable and the refuter never fires.\n- **#404** — engram transport drops `issue`; blocks #368 → T2.1 promotion.\n- **#410** — this memory materialization.\n\n## Epic #313 re-synced\nCritical path **inverted deliberately**: M4 (#396–#401) is now the highest-priority track, with M3 closure in parallel. Rationale recorded in the body: M4 is the hard gate blocking external adopters *today*; M3 residuals block reviewer *completeness*. The two touch disjoint surfaces, so nothing serialises them. Added a cross-cutting section for #404 (it belongs to no milestone yet blocks #368 → T2.1) and a standing rule for future agents: work described only in prose in that body is a bug — file an issue for it.\n\n## Process notes that cost time\n- Worktree-per-issue is the repo's workflow (`AGENTS.md`, load-bearing). `brain:ticket:start --worktree` fails in Claude-Code-on-the-web (`VCS token not found in .env` — the container has the GitHub MCP server, not a `gh` token); reproduce the branch convention manually from `deriveBranchType` + the 40-char slug rule.\n- Worktrees do not inherit `node_modules`; symlink it or nothing runs.\n- A combined branch being green does not prove each split half is green — run the full suite per worktree. Baseline on `e67dff8` is 2268 tests.\n- Commit signing is impossible in this container (0-byte `commit_signing_key.pub`, no private key, no `ssh-keygen`); merged history already carries unsigned Claude commits.\n- Deleting remote branches is blocked by the git proxy; `fix/issue-381-382-reviewer-roundtrip` is orphaned and needs deleting via the GitHub UI."}
{"id":"rec-5c2604d77275d577","ts":"2026-08-04T11:19:12Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"architecture","project":"brain","content":"**Defect class: render/parse asymmetry hidden by testing only the empty encoding (brain, #381)**\n\n`renderVerdict` serialised `findings` as a YAML block list; `parseVerdict` deserialised it with a scalar regex requiring content on the same line. Every non-empty findings list was silently dropped on the round trip.\n\n**The generalisable lesson is why the full suite stayed green.** The only encoding that round-tripped — `findings: []` — was the only encoding under test. Coverage over a serializer/parser pair proves nothing unless the tests exercise the *non-degenerate* shape.\n\nTwo practices adopted in the fix, worth repeating for any wire-format work in this repo:\n1. Tests drive the real `buildVerdict` → `renderVerdict` pair and parse the actual output. Hand-written fixture strings encode the author's belief about the format, which is exactly the thing under doubt.\n2. New tests were run against the OLD parser to confirm they fail (3 of 4 did). The 4th — the empty case — passes against both and was kept deliberately, as an in-repo record of the blind spot that hid the defect.\n\nRelated: this is the same \"green in test, inert in production\" class that M10 (#335) exists to close. It was found by reading the code, not by a failing test, which is itself evidence for #409 — brain runs the `lite` tier, so the whole `/2` path has no production exercise."}
{"id":"rec-b80476d69cd83dea","ts":"2026-08-04T11:19:13Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"architecture","project":"brain","content":"**M3 shipped consumers without producers — three wired-but-unreachable paths (brain)**\n\nAfter the M3 core merge, three code paths exist, are correct, and can never execute in production:\n\n1. **`follow_ups[]`** — `verdict.mjs:51` routes a `causal_disposition` of `pre-existing`/`base-only` into it, `schema-v2.mjs` admits both values, `reviewer-protocol.md:216,222` documents them. **No evaluator emits either.** `causal-admission.mjs:23` states why, correctly: none of the current checks compare against base closely enough to claim it honestly, and \"inventing the claim would be worse than omitting it\". The gap is that no check which *can* make the claim was ever built. Tracked in #408.\n2. **The refuter** (`evaluators/refuter.mjs`, #284) — runs only when a blocker carries `evidence_class: 'inferential'`. No evaluator produces inferential findings, so it is a documented no-op, deliberately wired so a future producer needs no second integration point. Same ticket, #408.\n3. **The entire `/2` tier** — activation routes `lite`/`standard` → `/1` and `regulated` → `/2`, and brain declares `lite` (ADR-0026, #358). Everything M3 built is covered by unit tests and by nothing else. Tracked in #409.\n\n**Consequence to carry forward:** #381 made `follow_ups` *readable*, not *populated* — it moved from broken to empty, not to working. Do not read a green suite or a merged PR as evidence that this feature works end to end.\n\n**Empirical evidence for #404, found while inspecting the store:** zero records across `2026-06.jsonl`, `2026-07.jsonl` and `2026-08.jsonl` carry an `issue` field. That is the \"~0% adoption\" #368 describes, and the cause is the transport dropping the field, not authors omitting it. Note `computeRecordId` DOES hash `issue` when present, so the format supports it — only the transport does not."}
{"id":"rec-c23912c259b37725","ts":"2026-08-04T11:19:13Z","actor":"claude/issue-396-upgrade-rollback","actorKind":"agent","type":"session_summary","project":"brain","content":"**Session summary: brain — #396 slice 1, restore-point rollback for brain:upgrade**\n\n## Goal\nPick up the brain 1.1 line and advance M4 — the epic #313 hard gate that blocks external\nadopters from upgrading safely. Owner chose M4 over M3 closure, then chose to deliver #396\nas a two-slice chain.\n\n## Accomplished\n- **#396 slice 1 shipped to review** — PR #412 (draft), chain `main ← feature/issue-396-rollback\n  ← PR #412 ← PR#2`. `copyManaged` now snapshots every path it may write before the first\n  write and restores those bytes on any throw; an incomplete rollback KEEPS its snapshot and\n  reports where. Symlinked managed paths are refused up front. Suite 2268 → 2282.\n- **Full SDD change dir written** at `openspec/changes/issue-396-upgrade-rollback/` —\n  proposal, spec (REQ-396-1..9 with scenarios and negative controls), design, tasks, plus\n  `brain-drafts/adr-0027-upgrade-rollback-is-restorable-not-atomic.md`.\n- **Three follow-up issues filed**: #413, #414, #415. Epic #313 re-synced with a status block,\n  a rewritten M4, and an explicit priority table for the next agent.\n\n## Discoveries\n1. **#396's stated exit criterion is unachievable as written.** `rename(2)` is atomic for ONE\n   path; the managed payload spans four disjoint locations (`brain/**`, `.github/**`,\n   `.gemini/**`, root files), so no single directory swap can commit it. Multi-file atomicity\n   needs a journal. Restated as *restorability*, with each residual gap named — ADR-0027 draft,\n   unsigned.\n2. **A signal handler cannot interrupt a synchronous fs loop.** Measured: SIGINT at 40ms into a\n   935ms sync copy loop → the loop ran to completion (i=60000) and the handler fired after;\n   the same loop with a `setImmediate` yield aborted at i=2251. So registering a handler is not\n   a mid-write abort — it suppresses the default terminate action, which is the only thing that\n   genuinely half-applies a tree. The real managed write is 366 files in ~23ms.\n3. **`spawnSync` returns `{status: null, signal}` when the child dies by signal.** A first\n   revision claimed, as measured, that a Ctrl-C during `npm install` was caught by the deferred\n   interrupt guard. It was not: `if (r.status !== 0)` fires first, reporting the interrupt as\n   \"install failed — check repo access\" and collapsing exit 130 into 1. Registering the handler\n   is what kept the parent alive to reach that branch, so the slice introduced that regression.\n   Fixed by testing `r.signal` before `r.status`. Recorded rather than rewritten out of history.\n4. **`existsSync` follows symlinks.** A dangling symlink at a managed path therefore read as\n   absent, was recorded as created-by-this-run, and was DELETED on rollback — while\n   `copyFileSync` wrote through the link to a target outside the repo, and the rollback reported\n   success. The feature corrupting what it exists to protect. Presence must be judged with\n   `lstat`.\n5. **A `finally` is the wrong place to discard a restore point.** It runs after `catch`, so it\n   deleted the snapshot on the incomplete-rollback path — the one branch where the snapshot is\n   the only surviving copy of the consumer's bytes, and the same branch whose message tells the\n   operator to go inspect them. Only a complete rollback may discard.\n6. **Annotating a caught error can throw.** Modules are strict mode, so\n   `err.rollbackIncomplete = …` raises a TypeError on a string, frozen Error or null — replacing\n   the real failure AND dropping the dirty-tree signal, which made the CLI report a clean\n   rollback over a dirty tree. `specialMerge` is caller-supplied on an exported API, so this\n   needs no brain-side bug to reach.\n7. **A test suite can be green and still miss the dominant shape.** The first test round always\n   left exactly ONE entry in `toCopy` — the one that throws — so plain-copy rollback was never\n   exercised, despite 364 of 366 real managed files being plain copies. Every rolled-back write\n   in the suite came from a merge fn. Fixed by sorting the write order and adding REQ-S6-10.\n8. **`brain:review` at `tier: lite` resolves to `brain-review/1`**, which has no\n   findings-producing evaluator: `findings: []` means \"nobody looked\", not \"the code is clean\".\n   `/2` — the half that wires causal-admission and the refuter — is `regulated`-only. This is\n   epic #313's #409 observed live, not a defect.\n9. **The reviewer identity is declared, never proven.** `identity.mjs` returns\n   `reviewerConfig.handle` and nothing cross-checks the token against it, so the §10 self-review\n   abstention and the anti-loop lock are forgeable. Filed as #413; sibling of #382.\n10. **`actor-check`'s `lite` evidence is a distinct ACT, not a state.** The approved-label event\n    must be strictly later than the PR's head commit, so every push invalidates the previous\n    approval. Working as designed — Q5's fix for the stale-green class — but it will read as a\n    broken CI to anyone who has not seen it.\n\n## Corrections made to my own earlier claims\n- Reported `TIER_PARAMS.lite.diffBudget` and `.artefacts` as unconsumed \"one rule, two\n  implementations\" divergences. **Both were wrong**: `diffBudget` is read by `brain-check.mjs`,\n  `brain-audit.mjs` and `merge-walk.mjs`; `artefacts` by `phase-order-check.mjs`. `check-refs`\n  demanding all four artefacts is a deliberately tier-independent STRUCTURAL check. The wrong\n  conclusion came from a mutilated grep (`rg -r` consumed the pattern as a replacement string).\n\n## Next steps\n- **#396 slice 2 — the on-disk journal.** The M4 hard gate stays CLOSED until it lands. It must\n  INVERT `createRestorePoint`'s entry-time clearing of a pre-existing snapshot: once recovery\n  exists, a leftover snapshot is evidence to replay, not debris. Flagged inline.\n- Human: re-apply `status:approved` on #396 after the last push; ratify or reject ADR-0027;\n  Tier-2 promotions for #397, #405 and #414.\n- Then #397/#398/#399, #401, #413, and M3 closure (#409 → #405 → #408).\n\n## Relevant files\n- `brain/scripts/lib/installer.mjs` — `createRestorePoint`, `annotateRollback`, guarded write loop\n- `brain/scripts/lib/installer.test.mjs` — REQ-S6-1..14\n- `brain/scripts/brain-upgrade.mjs` — signal-before-status, rollback reporting, interrupt handling\n- `docs/KNOWN-LIMITATIONS.md` — partial fix with all four residual gaps enumerated\n- `openspec/changes/issue-396-upgrade-rollback/` — proposal, spec, design, tasks, ADR-0027 draft","source":"plainfiles save on vm"}
{"id":"rec-7517830a9da07470","ts":"2026-08-04T11:19:14Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"session_summary","project":"brain","content":"**Session summary: brain — the governance-lock pair closed (#413 identity verification, #418 ADR-0026 Amendment 1)**\n\n## Goal\nWork in parallel with a second agent driving M4 (#396/#399 merged, #398 in flight). Picked the reviewer/governance track because it is file-disjoint from `installer.mjs`: #413, then #418 which #413 gated.\n\n## What shipped\n- **#413 → PR #424 (`e5e8e7c`).** The reviewer's handle was returned straight from config after an is-the-token-set check, so the §10 self-review abstention and the anti-loop lock both compared a CLAIMED identity — an author could review their own PR by pointing the token env var at their own token while config named a bot. `gatherIdentity` now resolves the token's real login via `whoami({ token })` at boot and fails closed on mismatch AND on verification error.\n- **#418 → PR #425 (`cae3a83`).** ADR-0026 Amendment 1, human-signed 04/08/2026: at `lite`, distinct-act re-arms only on a FOREIGN commit — authored by neither the approver nor a registered `governance.reviewActors` identity. Unresolvable authorship is foreign (fail closed).\n\n## Two discoveries that changed the shape of the work\n1. **#413's ticket suggested a new port verb.** Survey found `whoami` already existed on both providers WITH a contract row — the gap was only that it answered for the ambient CLI session, not for a given token, which is the wrong identity in exactly the forgery scenario. Widening the verb instead of adding one avoided an ADR-0020 amendment and kept the zero-arg path byte-identical. **Read the port before believing a ticket's premise about it.**\n2. **The #418 amendment silently required a guard nobody drafted.** Pre-amendment, \"an unreadable approval is not an approval\" was IMPLICIT: a missing label timestamp made `compareTimestamps` return null and the comparison fail. Amendment 1 adds a path (no foreign commit → satisfied) that never reaches that comparison, so a timestamp-less approval would have passed. Caught by an existing test, not by review — the invariant is now stated outright.\n\n## Residuals still open\n- **The `whoami` contract row is STALE.** `vcs-contract.md:26` still reads `() -> { username }` while shipped code accepts `token`. Replacement text sits in `openspec/changes/issue-413-reviewer-identity-verified/brain-drafts/`. Needs a Tier-2 promotion AND a new issue — #413 is closed, and every PR to `main` must close an open `status:approved` issue.\n- **#418's relief does not reach the agent loop.** The exemption keys off the login the PROVIDER attributes to a commit, never the commit header (trusting the header would make the exemption forgeable in exactly the way #413 closed). Agent sessions committing as `Claude <noreply@anthropic.com>` resolve to no account ⇒ foreign ⇒ still re-arm. GitLab is in the same position wholesale (`prCommits` resolves no logins there). Closing it is operator-side: attribute session commits to an exempt account, or register a real bot identity.\n\n## Test discipline applied (the epic's standing rule)\nEvery behavioural change verified RED against the pre-change code: 6 for #413, 5 for #418. In both PRs a control was deliberately kept GREEN on both old and new code and documented as such — #413's case-insensitivity check and #418's \"third-party push still re-arms\" / \"unresolvable authorship is foreign\". Those controls guard against an over-permissive fix, which a red-only suite cannot express.\n\n## Process\nWorktree per issue, branch names reproduced from `deriveBranchType` + the 40-char slug rule because `brain:ticket:start --worktree` fails without a VCS token in this container. Full suite run inside each worktree separately."}
{"id":"rec-444f162c91171ef9","ts":"2026-08-04T11:19:15Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"architecture","project":"brain","content":"**Amending an ADR is a three-step change — decision-gate and the AGENTS.md drift-guard both fire (brain, #418/#425)**\n\nLearned by hitting each step in sequence on PR #425, each surfacing only after the previous one landed. Nothing in the repo documented the chain.\n\n1. **Edit the ADR** under `brain/project/decisions/`. `brain/**` is human-only (`consolidation-protocol.md` zone map: *\"if the destination is brain/, the signature is human\"*), so an agent drafts into `openspec/changes/{id}/brain-drafts/` and the human promotes.\n\n2. **`decision-gate` then fails.** `checks/adr-presence.mjs` enforces an **unconditional** ADR ⇔ `brain/HOME.md` co-occurrence: touch one without the other and it refuses. It does NOT distinguish a new ADR from an amended one, so `HOME.md` must move even when it already indexes that ADR. Also human-only. In practice the gate was RIGHT — the index entry genuinely did not mention the amendment, and an index that misdescribes the document is what that gate exists to prevent. (ADR-0026's own Consequences section already recorded this check's divergence from its documentation as a known residual; it read as a footnote, not as an operational warning.)\n\n3. **The `AGENTS.md` drift-guard then fails.** `AGENTS.md` is COMPILED from 5 `SOURCE_DOCS` (`harness/backends/antigravity.mjs`), one of which is `brain/HOME.md`. Touching the index leaves the committed `AGENTS.md` byte-different from its sources. Fix by REGENERATING — `AGENT_PLATFORM=antigravity node brain/scripts/harness/cli.mjs init` — never by hand-editing, which is precisely what the guard exists to catch. `AGENTS.md` lives at the repo root, not under `brain/`, and is a generated artifact, so an agent may regenerate it.\n\n**Plan all three up front.** Doing only step 1 produces a red PR, and fixing that produces a second red PR. None of the three gates is misfiring — the chain is the real coupling between doctrine, its index, and the compiled artifact."}
{"id":"rec-59604a4b621b13b1","ts":"2026-08-04T11:19:15Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"architecture","project":"brain","content":"**How to capture memory when the engram binary is absent (brain, #410/#426)**\n\n`memory:share` and `brain:save` both fail with `engram binary not found` in the Claude-Code-on-the-web container, so nothing is captured automatically and `git status -- .memory` stays clean. #410 established the workaround but recorded it **only in a PR body and commit message** — not in a memory record — so the next agent to capture a session would hit the same wall with nothing to warn them. This record closes that loop.\n\n**The procedure:**\n1. Author records through `buildRecord()` + `validateRecord()` from `brain/scripts/memory/lib/format.mjs`. Pass `title` separately from `content`; `buildRecord` folds it into a bold Markdown prefix BEFORE hashing, which is what makes ids reproducible across machines.\n2. Append the serialized records to `.memory/records/YYYY-MM.jsonl`.\n3. Run `npm run memory:reindex` — explicitly backend-agnostic (`memory/cli.mjs`: *\"regenerate .memory/index.jsonl from .memory/records/ alone\"*), so it works with no engram present.\n\n**Never hand-write ids.** They are `\"rec-\" + sha256(canonicalJson(hashInput))[:16]` and `rebuildIndex` recomputes and verifies every one on each reindex. A first attempt using random ids was rejected outright:\n`rebuildIndex: id mismatch at 2026-08.jsonl:2 — stored id '...' does not match the recomputed id '...' (tampered or stale record)`\nThat is the REQ-C4-1 id-equality check working. The store defends itself against invented content.\n\n**Omit `issue` until #404 is fixed.** `computeRecordId` DOES hash the field when present, so the format supports it — but the transport drops it, so a populated `issue` would produce an id mismatch on the first engram round trip and poison the integrity check. Confirmed empirically: zero records across `2026-06/07/08.jsonl` carry the field, which is the \"~0% adoption\" #368 describes.\n\n**A memory PR needs its own issue.** `governance.yml` requires every PR to `main` to carry `Closes|Fixes|Resolves #N` on a `status:approved` issue, and there is no exemption for memory. Historically memory rode inside work PRs; #410 and #426 each had to be filed purely to give a memory PR something legitimate to close. The pattern deserves a decision."}
{"id":"rec-97c2a165284b15bb","ts":"2026-08-04T12:21:19Z","actor":"@legacy","actorKind":"human","type":"architecture","project":"brain","content":"**#397 slice 3: AGENTS.md regenerated not copied, .gemini merged — PR #434 green**\n\n**What**: Slice 3 shipped — commit `febb213` on `fix/issue-397-agents-regenerate-gemini-merge`, PR #434 chained onto #429's branch (correct child-branch chaining this time). All 8 governance checks PASS. Tasks 2.4, 2.6, 2.7, 3.5, 3.6 checked. Suite 2368/2368, review surface 54 lines.\n\n**Why**: REQ-397-3 (.gemini merged) and REQ-397-4 (AGENTS.md regenerated, never copied).\n\n**Where**: `brain/core/managed-paths.mjs` (AGENTS.md removed from `managed`), `brain/scripts/brain-upgrade.mjs` (`.gemini/settings.json` → ALL_MERGES; step 4 regeneration), `managed-paths.test.mjs`, `brain-upgrade.test.mjs`.\n\n**Learned** — the load-bearing ones:\n\n1. **`antigravity.mjs#init()` writes BOTH `AGENTS.md` AND `.gemini/settings.json`.** Calling it plainly to satisfy REQ-397-4 overwrites the `.gemini` merge REQ-397-3 performs earlier in the SAME run. Fix: pass `_writeGeminiSettings: () => {}` to neutralise that seam rather than avoiding the call, so AGENTS.md still goes through the generator brain's drift-guard checks against. **Anyone touching AGENTS.md regeneration must check this seam.**\n\n2. **Regeneration MUST run after the managed copy.** It reads the four methodology docs under `brain/core/methodology/` that the copy just updated; running earlier compiles the previous release. Roots differ on purpose: CODE from the script's own sibling (`new URL('./harness/backends/antigravity.mjs', import.meta.url)`), DATA from `ROOT` (`_repoRoot: ROOT`).\n\n3. **Removing a path from `managed` does NOT strand fresh consumers here** — `bootstrap.sh:263` runs `harness/cli.mjs init`, which GENERATES AGENTS.md. Delivery was never the managed copy. Always verify the fresh-install path before removing a managed entry.\n\n4. **`mergeClaudeSettings` is generic despite its name** — it merges any `{hooks: {event: [entries]}}` shape additively with consumer keys preserved. `.gemini/settings.json` has exactly that shape, so it reuses the function directly. Not renamed: the rename would be a wider diff than the decision.\n\n5. **Task 2.7 was free.** The #399 merge pre-flight is `preflightMergeTargets({mergePaths: Object.keys(mergeMap)})`, so registering a merge registers its pre-flight. Now locked in by a test against a future hand-written second list.\n\n6. **The REGENERATE strategy breaks slice 1's \"every classified path is in managed\" invariant BY DESIGN** — the absence IS the decision. Exemption scoped to REGENERATE only, plus a converse test asserting a REGENERATE row is NEVER in `managed`, so the halves cannot drift.\n\n**Next**: slice 4 — REQ-397-6 already-clobbered detection (task 2.8, tests 3.8/3.9) + docs (4.1, 4.2). Spec leaves the detection MECHANISM open (git history vs shipped hash manifest); the binding constraint is Scenario 2 — it must NOT fire for a consumer who never had anything of their own to lose.","source":"provenance unknown — migrated from engram chunk obs-1180467dca938249"}
{"id":"rec-1d1d449e39dc26ee","ts":"2026-08-04T13:58:29Z","actor":"@legacy","actorKind":"human","type":"session_summary","project":"brain","content":"**Session summary: brain**\n\n## Goal\nStart a brain session and continue #397 (M4 — plain-copy clobber asymmetry). The design was already ratified and pushed; the job was the implementation, delivered as a 4-slice chained-PR sequence.\n\n## Instructions the owner gave\n- Chain strategy: **feature-branch-chain** (tracker `feature/issue-397-clobber-asymmetry`), so #397 stays open until every slice lands. Only the tracker carries `Closes #397`.\n- Slices 1+2 shipped together in one PR; slices 3 and 4 as child PRs.\n- REQ-397-6 scope: **regenerate-and-compare, narrowed to AGENTS.md**, with the non-generated paths tracked as a follow-up.\n\n## Accomplished\nComplete 4-slice chain, all green:\n- **#429** — slices 1+2 (`e18e407`, `351a1f2`): signed classification as data (`STRATEGY`/`managedStrategy`), three-way modification detection via `readOutgoing()`, REFUSE gate + per-path `--force-managed`.\n- **#434** — slice 3 (`febb213`): `.gemini/settings.json` merged; `AGENTS.md` off the copy set and regenerated.\n- **#437** — slice 4 (`fd9d0b2`): already-clobbered detection (`brain/scripts/lib/agents-clobber.mjs`), docs closure.\n- All three PRs: 8/8 governance checks PASS, `type:bug`, correct bases.\n- Suite grew 2349 → 2381, all green. Review surface: 317 + 54 + 55 lines, no `size:exception` anywhere.\n- Filed **#433** (session:start 20-minute block) and **#436** (shipped-hash manifest, Tier-2 follow-up).\n\n## Discoveries — repo and tooling\n- **`actor-check.mjs:405` — issue-link is ASYMMETRIC by base.** `base=main` REQUIRES `Closes|Fixes|Resolves #N`; `Part of #N` is rejected there. `base!=main` also accepts `Part of #N`. This is what makes the feature-branch chain the only way to ship slices without auto-closing the parent.\n- **`brain:check` issueLink inspects COMMITS on the branch, not the working tree.** \"Refs #N\" does NOT satisfy it.\n- **`gh pr edit` is broken here** — fails with `GraphQL: Projects (classic) is being deprecated ... (repository.pullRequest.projectCards)` and applies NOTHING. Workaround: `jq -n --arg t \"<title>\" --rawfile b body.md '{title:$t,body:$b}' | gh api -X PATCH repos/csrinaldi/brain/pulls/<N> --input -`. Always verify with `gh pr view`.\n- **`sd` is not installed** and invoking it triggers a sudo prompt that fails. Use the Edit tool for in-place source mutation.\n- **brain's diff-size counter excludes `**/*.test.mjs`** via `governance.ignoreList`. Run `git diff --numstat <base> | node brain/scripts/vcs/diff-size-count.mjs` for the number the gate uses.\n- **`npm run session:start` takes ~20 minutes**, blocked in `memory/cli.mjs import` (full re-import of 1722 records / 18 MB, no watermark), emitting nothing until done. Exit 0. → #433.\n\n## Discoveries — technical, load-bearing\n- **`antigravity.mjs#init()` writes BOTH `AGENTS.md` AND `.gemini/settings.json`.** Calling it plainly to satisfy REQ-397-4 overwrites the `.gemini` merge REQ-397-3 performs earlier in the SAME run. Fix: `_writeGeminiSettings: () => {}`.\n- **Regeneration must run AFTER the managed copy** — it reads methodology docs the copy just updated. CODE from the script's own sibling, DATA from `ROOT`.\n- **Removing `AGENTS.md` from `managed` strands nobody** — `bootstrap.sh:263` runs `harness/cli.mjs init`, which generates it. Always check the fresh-install path before removing a managed entry.\n- **`strategyFor` must give exact literals priority over globs** — `brain/scripts/ci/gitlab-governance.yml` is REFUSE under the `brain/scripts/**` COPY glob.\n- **Git history is unusable for clobber detection**: brain's own `AGENTS.md` changed 16 times across 17 tags, so a history rule fires for nearly every consumer — the exact thing REQ-397-6 Scenario 2 forbids. Measured, then rejected.\n- **A negative control cannot be proven RED by deleting the feature.** With no detector, nothing fires, so it passes for the wrong reason. Prove it by MUTATION — removing the Scenario-2 gate failed exactly the two negative controls and nothing else.\n- **Assert behaviour, not spelling.** A test pinning the literal `--force-managed` in the tag-parse line broke on a `VALUE_FLAGS` refactor while the invariant held. Rewritten to assert the missing-tag guard fires.\n\n## Process mistake worth remembering\nPR #429 was open as **design-only, \"slice 1 of 2\"**, with implementation planned as a CHILD PR. I committed the implementation onto the same branch, folding both into #429. Once pushed, splitting would need a force-push on a published branch — `AGENTS.md` Tier 3, prohibited. Remedy was amending the PR description honestly, noting design and implementation stay separable by commit. **Check for an existing open PR on the branch BEFORE the first implementation commit.**\n\n## Next Steps\n- Merge the chain bottom-up: #437 → #434 → #429 → tracker → `main` with `Closes #397`.\n- Run `npm run memory:share` before the tracker merges (memory-gate checklist item is still unchecked on all three PRs).\n- **Task 3.7 stays PARTIAL**: the REFUSE gate needs `outgoing !== incoming`, unreachable via `--no-install`. Belongs to #401's container harness.\n- #436 needs a Tier-2 draft + signature before implementation.\n- #433 (session:start) is unassigned and unstarted.\n\n## Relevant Files\n- `brain/core/managed-paths.mjs` — `STRATEGY` + `managedStrategy`, the signed table as data; `AGENTS.md` removed from `managed`\n- `brain/scripts/lib/installer.mjs` — `strategyFor()`, `readOutgoing()`, `copyManaged` three-way + REFUSE gate\n- `brain/scripts/brain-upgrade.mjs` — step 0 snapshot, `--force-managed`, refusal report, step 4 regeneration + clobber detection\n- `brain/scripts/lib/agents-clobber.mjs` — new pure detector (REQ-397-6)\n- `docs/KNOWN-LIMITATIONS.md` — #397 entry closed with three named residuals\n- `openspec/changes/issue-397-clobber-asymmetry/` — spec.md carries the REQ-397-6 narrowing and its justification; tasks.md fully rendered","source":"provenance unknown — migrated from engram chunk obs-ecba78e9326e2794"}
{"id":"rec-8af5bbfb7b15780a","ts":"2026-08-04T01:36:49Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"session_summary","project":"brain","content":"**Session summary: brain — the governance-lock pair closed (#413 identity verification, #418 ADR-0026 Amendment 1)**\n\n## Goal\nWork in parallel with a second agent driving M4 (#396/#399 merged, #398 in flight). Picked the reviewer/governance track because it is file-disjoint from `installer.mjs`: #413, then #418 which #413 gated.\n\n## What shipped\n- **#413 → PR #424 (`e5e8e7c`).** The reviewer's handle was returned straight from config after an is-the-token-set check, so the §10 self-review abstention and the anti-loop lock both compared a CLAIMED identity — an author could review their own PR by pointing the token env var at their own token while config named a bot. `gatherIdentity` now resolves the token's real login via `whoami({ token })` at boot and fails closed on mismatch AND on verification error.\n- **#418 → PR #425 (`cae3a83`).** ADR-0026 Amendment 1, human-signed 04/08/2026: at `lite`, distinct-act re-arms only on a FOREIGN commit — authored by neither the approver nor a registered `governance.reviewActors` identity. Unresolvable authorship is foreign (fail closed).\n\n## Two discoveries that changed the shape of the work\n1. **#413's ticket suggested a new port verb.** Survey found `whoami` already existed on both providers WITH a contract row — the gap was only that it answered for the ambient CLI session, not for a given token, which is the wrong identity in exactly the forgery scenario. Widening the verb instead of adding one avoided an ADR-0020 amendment and kept the zero-arg path byte-identical. **Read the port before believing a ticket's premise about it.**\n2. **The #418 amendment silently required a guard nobody drafted.** Pre-amendment, \"an unreadable approval is not an approval\" was IMPLICIT: a missing label timestamp made `compareTimestamps` return null and the comparison fail. Amendment 1 adds a path (no foreign commit → satisfied) that never reaches that comparison, so a timestamp-less approval would have passed. Caught by an existing test, not by review — the invariant is now stated outright.\n\n## Residuals still open\n- **The `whoami` contract row is STALE.** `vcs-contract.md:26` still reads `() -> { username }` while shipped code accepts `token`. Replacement text sits in `openspec/changes/issue-413-reviewer-identity-verified/brain-drafts/`. Needs a Tier-2 promotion AND a new issue — #413 is closed, and every PR to `main` must close an open `status:approved` issue.\n- **#418's relief does not reach the agent loop.** The exemption keys off the login the PROVIDER attributes to a commit, never the commit header (trusting the header would make the exemption forgeable in exactly the way #413 closed). Agent sessions committing as `Claude <noreply@anthropic.com>` resolve to no account ⇒ foreign ⇒ still re-arm. GitLab is in the same position wholesale (`prCommits` resolves no logins there). Closing it is operator-side: attribute session commits to an exempt account, or register a real bot identity.\n\n## Test discipline applied (the epic's standing rule)\nEvery behavioural change verified RED against the pre-change code: 6 for #413, 5 for #418. In both PRs a control was deliberately kept GREEN on both old and new code and documented as such — #413's case-insensitivity check and #418's \"third-party push still re-arms\" / \"unresolvable authorship is foreign\". Those controls guard against an over-permissive fix, which a red-only suite cannot express.\n\n## Process\nWorktree per issue, branch names reproduced from `deriveBranchType` + the 40-char slug rule because `brain:ticket:start --worktree` fails without a VCS token in this container. Full suite run inside each worktree separately."}
{"id":"rec-54342d4e2723237d","ts":"2026-08-04T01:36:49Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"architecture","project":"brain","content":"**Amending an ADR is a three-step change — decision-gate and the AGENTS.md drift-guard both fire (brain, #418/#425)**\n\nLearned by hitting each step in sequence on PR #425, each surfacing only after the previous one landed. Nothing in the repo documented the chain.\n\n1. **Edit the ADR** under `brain/project/decisions/`. `brain/**` is human-only (`consolidation-protocol.md` zone map: *\"if the destination is brain/, the signature is human\"*), so an agent drafts into `openspec/changes/{id}/brain-drafts/` and the human promotes.\n\n2. **`decision-gate` then fails.** `checks/adr-presence.mjs` enforces an **unconditional** ADR ⇔ `brain/HOME.md` co-occurrence: touch one without the other and it refuses. It does NOT distinguish a new ADR from an amended one, so `HOME.md` must move even when it already indexes that ADR. Also human-only. In practice the gate was RIGHT — the index entry genuinely did not mention the amendment, and an index that misdescribes the document is what that gate exists to prevent. (ADR-0026's own Consequences section already recorded this check's divergence from its documentation as a known residual; it read as a footnote, not as an operational warning.)\n\n3. **The `AGENTS.md` drift-guard then fails.** `AGENTS.md` is COMPILED from 5 `SOURCE_DOCS` (`harness/backends/antigravity.mjs`), one of which is `brain/HOME.md`. Touching the index leaves the committed `AGENTS.md` byte-different from its sources. Fix by REGENERATING — `AGENT_PLATFORM=antigravity node brain/scripts/harness/cli.mjs init` — never by hand-editing, which is precisely what the guard exists to catch. `AGENTS.md` lives at the repo root, not under `brain/`, and is a generated artifact, so an agent may regenerate it.\n\n**Plan all three up front.** Doing only step 1 produces a red PR, and fixing that produces a second red PR. None of the three gates is misfiring — the chain is the real coupling between doctrine, its index, and the compiled artifact."}
{"id":"rec-b6ad580b8072b15d","ts":"2026-08-04T01:36:49Z","actor":"claude/brain-architecture-review-qe5bo2","actorKind":"agent","type":"architecture","project":"brain","content":"**How to capture memory when the engram binary is absent (brain, #410/#426)**\n\n`memory:share` and `brain:save` both fail with `engram binary not found` in the Claude-Code-on-the-web container, so nothing is captured automatically and `git status -- .memory` stays clean. #410 established the workaround but recorded it **only in a PR body and commit message** — not in a memory record — so the next agent to capture a session would hit the same wall with nothing to warn them. This record closes that loop.\n\n**The procedure:**\n1. Author records through `buildRecord()` + `validateRecord()` from `brain/scripts/memory/lib/format.mjs`. Pass `title` separately from `content`; `buildRecord` folds it into a bold Markdown prefix BEFORE hashing, which is what makes ids reproducible across machines.\n2. Append the serialized records to `.memory/records/YYYY-MM.jsonl`.\n3. Run `npm run memory:reindex` — explicitly backend-agnostic (`memory/cli.mjs`: *\"regenerate .memory/index.jsonl from .memory/records/ alone\"*), so it works with no engram present.\n\n**Never hand-write ids.** They are `\"rec-\" + sha256(canonicalJson(hashInput))[:16]` and `rebuildIndex` recomputes and verifies every one on each reindex. A first attempt using random ids was rejected outright:\n`rebuildIndex: id mismatch at 2026-08.jsonl:2 — stored id '...' does not match the recomputed id '...' (tampered or stale record)`\nThat is the REQ-C4-1 id-equality check working. The store defends itself against invented content.\n\n**Omit `issue` until #404 is fixed.** `computeRecordId` DOES hash the field when present, so the format supports it — but the transport drops it, so a populated `issue` would produce an id mismatch on the first engram round trip and poison the integrity check. Confirmed empirically: zero records across `2026-06/07/08.jsonl` carry the field, which is the \"~0% adoption\" #368 describes.\n\n**A memory PR needs its own issue.** `governance.yml` requires every PR to `main` to carry `Closes|Fixes|Resolves #N` on a `status:approved` issue, and there is no exemption for memory. Historically memory rode inside work PRs; #410 and #426 each had to be filed purely to give a memory PR something legitimate to close. The pattern deserves a decision."}
{"id":"rec-6c297253b3e65742","ts":"2026-08-04T14:24:59Z","actor":"@legacy","actorKind":"human","type":"discovery","project":"brain","content":"**Two silent traps: memory:share in a worktree, and a conflicting PR running ZERO checks**\n\n**What**: Two failure modes hit while closing #397, both of which report success or look like something else entirely.\n\n**1. `npm run memory:share` from a WORKTREE is a silent no-op.**\n`.engram` is a symlink to `.memory` and is **gitignored** (`.gitignore:68`), so it does not exist in a freshly created worktree. The command then creates a REAL `.engram/` directory, exports chunks into it, prints `Created chunk <id> / Observations: N`, and materializes **nothing** into `.memory/records/`. `git status` stays clean.\n- Symptom: share reports success, `git status` shows no changes.\n- Fix: `rm -rf .engram && ln -s .memory .engram` in the worktree, or run it from the main worktree.\n- Measured difference: bogus run reported 1 observation; correct run reported 1610 observations / 4 sessions / 16 mutations and appended 421 records to `2026-07.jsonl` + 10 to `2026-08.jsonl`.\n\n**2. A PR with merge conflicts runs ZERO GitHub Actions checks.**\n`governance.yml` triggers on `pull_request`, and GitHub cannot compute a merge commit for a conflicting PR — so no workflow runs at all. `gh pr checks` returns *\"no checks reported on the branch\"* and exits 1.\n- This reads like a CI outage or a workflow-trigger misconfiguration, and sends you to inspect `on:` blocks. **Check `gh pr view <N> --json mergeable,mergeStateStatus` FIRST.** `CONFLICTING`/`DIRTY` is the real answer.\n- Root cause of my instance: I ran the pre-flight merge verification, THEN added a commit (memory materialization) that touched `.memory/index.jsonl` — the file most likely to conflict. **Re-verify mergeability after any commit that lands after the merge check.**\n\n**3. The correct resolution for a `.memory/index.jsonl` conflict** (the machinery #330 built):\n`.memory/records/*.jsonl` carry `merge=union` in `.gitattributes` and auto-merge. `index.jsonl` is a **DERIVED projection**, not a source — never pick a side. Run `npm run memory:resolve-index`: it detects the conflict, regenerates the index from `records/`, and stages it (`✓ index conflict resolved — 2015 record(s) regenerated from records/ and staged`). Choosing a side on a derived file is how an index silently stops matching its records.\n\n**Where**: `.gitignore:68`, `.gitattributes` (`/.memory/records/*.jsonl merge=union`), `brain/scripts/memory/cli.mjs` (`share`, `resolve-index`), `.github/workflows/governance.yml`.\n\n**Learned**: both traps share a shape — the failure presents as success or as an unrelated problem. Verify the *effect* (`git status` after share; `mergeable` before blaming CI), never the *exit message*.","source":"provenance unknown — migrated from engram chunk obs-f34ad7f333dfed7a"}
{"id":"rec-8117244299f58fe2","ts":"2026-08-04T15:09:52Z","actor":"@legacy","actorKind":"human","type":"architecture","project":"brain","content":"**#401: M4 danger-path e2e — local git remote, and the harness bug that made the clobber test vacuous**\n\n**What**: Shipped #401 — the four M4 danger paths as a live e2e suite. Commit `c563345`, PR #441, all 9 checks green including the new `m4-danger-paths` job (1m26s in real CI). 21 assertions, 4 scenarios, zero product files touched.\n\n**Where**: `test/upgrade/danger-paths.sh` (host runner), `test/upgrade/danger-paths-in-container.sh` (scenarios), `.github/workflows/m4-danger-paths.yml`, `package.json` (`test:danger-paths`), `test/upgrade/README.md`.\n\n**Learned — the load-bearing findings:**\n\n1. **The existing `run.sh` harness CANNOT test the REFUSE gate, structurally.** It does `npm i -D brain#TO` and THEN invokes `brain:upgrade`. Three-way detection (#397) reads the OUTGOING package *before* the install, so there `outgoing == incoming` → permanently degraded → the gate can never fire. The clobber scenario would have passed vacuously. If anyone extends `run.sh` for danger paths, this is the trap.\n\n2. **`resolveInstallUrl` passes `git+file://` through unchanged** (its \"unknown form, already prefixed\" fallback branch, `installer.mjs:1483`). That is what lets a test build a bare git repo in-container and have `brain:upgrade` do its own REAL `npm i` against it — genuine install path, `outgoing != incoming`, and the suite tracks the BRANCH rather than published tags.\n\n3. **npm installs from a local bare git repo carry `.github/**`** — verified by spike before writing the harness. brain has no `files` field in package.json and `private: true` does not block git installs.\n\n4. **Mutation testing revealed message-vs-behaviour divergence twice:**\n   - REFUSE gate removed → the run STILL printed \"refused\" and STILL exited 1, having written the files first. Every message-based assertion passed; only the byte-level check caught it.\n   - Corrupt-JSON pre-flight removed → the file was STILL named and the run STILL stopped, from a later merge failure. The pre-flight's value is failing EARLY so `--skip-merge` works. Only behavioural assertions caught it.\n   **Assert bytes and exit codes, not messages.**\n\n5. **A new job in `governance.yml` is REJECTED by drift-guards** (`GOVERNANCE_JOBS`, `GATE_MATRIX`, job-order tests in `governance-checks.test.mjs` / `governance-tiers.mjs`). That job set is a ratified contract (ADR-0015) where every entry is a governance gate with a tier-resolved policy — and the file is a MANAGED path vendored to consumers. Maintainer-only CI belongs in its OWN workflow file, which is not managed and therefore never vendored.\n\n6. **brain's own diff budget is 1000, not 400.** `brain.config.json` declares `tier: \"lite\"`, and `tierParams` maps lite→1000, standard→400, regulated→200. The PR template's 400 is the standard default.\n\n7. **`docker run ... bash -s < script` silently does NOTHING without `-i`.** Exit 0, no output. Cost a debugging cycle.\n\n8. **Scenario 1 deliberately departs from the ticket's \"mid-upgrade kill\"**: a real SIGKILL cannot be aimed inside a synchronous write batch. Deterministic equivalent that exercises the same restore-point machinery — TO ships a file under a new directory, the consumer has a FILE where that directory must go → `mkdirSync` throws ENOTDIR mid-loop after earlier writes landed.\n\n**Next**: #441 awaiting merge. Once merged, epic #313's M4 hard gate is checkable — external adoption opens when this suite is green.","source":"provenance unknown — migrated from engram chunk obs-fa76c23ab3ecc92d"}
{"id":"rec-d33d6d43843dbbfe","ts":"2026-08-04T15:35:08Z","actor":"@legacy","actorKind":"human","type":"discovery","project":"brain","content":"**engram import is ~0.6s per record — one execFileSync spawn each; blocks session:start and every git pull**\n\n**What**: Measured the engram import on `main` with 1722 records / 18 MB:\n\n| command | time |\n|---|---|\n| `memory/cli.mjs import` | **1053.30 s** (17.5 min) |\n| `memory/cli.mjs resolve-index` | 0.13 s |\n\n**0.61 seconds per record.**\n\n**Why**: `backends/engram.mjs:542` — `importMemory` loops every record calling `_engramSave(...)` once each. `backends/engram.mjs:1017` — `_defaultEngramSave` is `execFileSync(engram, [\"save\", …])`. **One process spawn per observation.**\n\n**Where it hurts — TWO triggers, same root cause:**\n1. `session-start.mjs:274` (`step2HydrateEngram`) — spawns the import synchronously and blocks every later step. Prints nothing until done.\n2. `brain/scripts/hooks/post-merge:30` — runs the same import after **every `git pull` or `git merge`**, with output redirected to `/dev/null`. Harder to diagnose: `git pull` just hangs with no signal, and the instinct is to blame the network, not a local hook. The `|| true` protects against failure, not duration.\n\n**Learned — two SEPARABLE defects, easily conflated:**\n- **No watermark** → re-imports all 1722 records every invocation (repeats finished work).\n- **Per-record process spawn** → each record costs ~0.61 s regardless.\n\nFixing only the watermark is NOT enough: a perfect incremental import still pays ~0.6 s per *new* record, and a first-time import in a fresh clone — the adopter path M4 exists for — would still take 17 minutes. The load-bearing fix is a **batch/bulk save path** (one engram invocation for N observations, or a direct store write).\n\n**Correction worth keeping**: `resolve-index` in the post-merge hook is technically redundant (git never fires `post-merge` on a failed merge, so there is never a conflict to resolve there) but costs **0.13 s** — it is NOT part of the problem. I initially framed it as if it mattered; it does not.\n\n**Tracked**: issue #433, retitled to name the real root cause. Both triggers and the measurement are in a comment there.","source":"provenance unknown — migrated from engram chunk obs-73750138528bc32e"}
{"id":"rec-540cba535b6569d1","ts":"2026-08-11T11:14:35Z","actor":"fix/issue-530-memory-capture-reachable","actorKind":"agent","type":"session_summary","project":"brain","content":"**brain: nueve PRs, 62% → 92% del lazo operativo**\n\nNine PRs to main: the operational loop went 62% to 92%, and GitLab stopped being a separate number.\n\nLanded (merge order): #506 rev-bound per head (PR 527) · #528 issueCreate + the port-level refusal that keeps the approval label human (531) · #459 brain:epic:map, dependencies as declared data (532) · #479+#475 the audit authenticates through VCS_TOKEN (534) · #480 the workflow-auth guard derives its rule from the invoked code (536) · #130 one rule, two pipelines (537) · #518 the audited set is the whole first-parent line (538) · #529 the memory-gate ruling (539).\n\nMeasure: 13 capabilities of the loop a user needs on a project X — 11 complete, 2 partial (#530 the writer, #533 who-is-executing), 0 absent. Both providers identical since #130.\n\nStanding constraints: the agent never applies status:approved (#124); brain/** is Tier 2 so doctrine ships as a draft and the human's commit is the signature; issue-link on base main requires a closing keyword.\n\nThis record was written by `npm run memory:save` — the verb #530 makes reachable. Before it, nothing in this environment could write a durable record at all.","issue":313,"source":"plainfiles save on vm"}
{"id":"rec-79156363ba72959d","ts":"2026-08-11T11:14:35Z","actor":"fix/issue-530-memory-capture-reachable","actorKind":"agent","type":"discovery","project":"brain","content":"**#530: el escritor existía; faltaba alcanzarlo**\n\nThe capture path was UNREACHABLE, not missing.\n\nBefore: a records-only writer needing no engram EXISTS (plainfiles.mjs#save, fully tested) · not reachable by default (MEMORY_BACKEND defaults to engram) · no npm verb (index/share/pull/reindex existed, save did not) · `memory save \"t\" \"c\"` threw `canonicalJson: unsupported value type 'undefined'`, naming neither field nor flag · and engram's own refusal pointed at `engram save`, a binary `command -v` cannot find here.\n\nA working writer existed and every signpost pointed elsewhere. Ruling (c), and smaller than the ticket assumed: reachability, not new machinery.\n\nThe asymmetry that is the fix: `project` is a FACT the config carries, so it is DERIVED (slug → name → directory). `type` is a CHOICE among seven, so it is REFUSED by name with the list. Defaulting type would stamp fabricated meaning onto a durable record. Derive facts, never opinions.\n\nWhy the crash had zero coverage: every existing plainfiles.save test passed BOTH fields. Green tests, unusable verb.\n\nFound while guarding it: `t(key, params, { lang })` silently ignores the option — it is `locale`. My first reachability test looped en/es passing `lang` and asserted the SAME catalogue twice: N=1 wearing an N=2 loop. The guard now asserts the two strings DIFFER.","issue":530,"source":"plainfiles save on vm"}
{"id":"rec-74746a0213c05bfe","ts":"2026-08-11T11:14:35Z","actor":"fix/issue-530-memory-capture-reachable","actorKind":"agent","type":"decision","project":"brain","content":"**#529: memory-gate es repo-scoped; el orden es la sustancia**\n\nmemory-gate is repo-scoped: ruling (1) now, sequenced to recency after the writer works.\n\nCost stated with the choice: the gate still cannot notice the next outage; what it buys is that nobody reads it as a per-change guarantee while it cannot be one. (2) recency lands after #530, and NOT before skip:memory-gate exists in code.\n\nWhy the order is the substance, measured: option (2) fails a PR whose base postdates the newest session_summary. The newest was 2026-08-04 and 34 merges had landed since — it would have blocked ALL of them. And the promised override does not exist: no code path checks skip:memory-gate. So (2) then would block with NO escape, and the only way out would be removing the gate.\n\nSecond defect found while measuring: the invariant table called row 3 \"Hard with override\". Neither half was true. The file already contradicted itself — the repo-global scope sat in the metrics caveats 120 lines BELOW the table, which is not where a reader forms the belief.","issue":529,"source":"plainfiles save on vm"}
{"id":"rec-ff7166dd01d20478","ts":"2026-08-11T11:15:00Z","actor":"fix/issue-530-memory-capture-reachable","actorKind":"agent","type":"discovery","project":"brain","content":"**#518: un tercio de main sin auditar**\n\nA third of main was never audited, and the design question that held the fix made the model SIMPLER.\n\nThe walk selected `git log --first-parent --merges`. `--merges` means more than one parent, so a squash was never enumerated — not evaluated and passed: never looked at. Measured on origin/main over 60 days: 112 first-parent commits, 79 enumerated, 33 never audited (32 carrying a (#N) reference).\n\nIt compounded: a window with no findings exits 0, the cursor advances, and since the cursor only moves forward every unenumerated commit became PERMANENTLY un-re-auditable. No remediation path either — [FAIL-SHA] can only nominate a commit the audit enumerated.\n\nThe held question: every exemption predicate is built on `<sha>^1..<sha>` and nothing had decided what that means with no second parent. Answer: `^1` resolves for ANY non-root commit, and for a linear one it is simply its own diff — less ambiguous than a merge's contribution. So sign/netPresent/netAddFull/addedPathsAbsentAt needed NO change. Only the three ENUMERATORS moved, and together — widening one alone leaves the two sides disagreeing about what a window contains, worse than either narrow one.\n\nConsequence: every commit on the integration line is now governed, including direct pushes. --first-parent untouched. Existing history is not re-audited.","issue":518,"source":"plainfiles save on vm"}
{"id":"rec-3453b4b72f76e999","ts":"2026-08-11T11:15:01Z","actor":"fix/issue-530-memory-capture-reachable","actorKind":"agent","type":"discovery","project":"brain","content":"**#479/#509: dos seams que parecían cerrados**\n\nTwo seams that looked closed and were not.\n\n#479 — brain declares ONE neutral credential and nothing used it. getVcs() bound a credential only when a caller passed `identity`, and measured, exactly 1 of 20+ call sites does. The same audit authenticated two ways depending on provider with nothing asserting they agreed. Fix belongs in getVcs(), not threaded through the audit path.\n\nThe residual, visible only after removing the accident hiding it: ci-context.mjs imported prView STRAIGHT from providers/github.mjs, bypassing getVcs() and its credential resolution. It looked fine because every job needing the PR body also set GH_TOKEN, so gh authenticated by accident. Declaring VCS_TOKEN alone turned issue-link red.\n\n#509 — promoting doctrine re-derives a written cascade every time. brain:promote handles the ADR shape only, so #529's methodology-doc ruling shipped as a bespoke script that missed regenerating AGENTS.md; CI caught it on the human's signing commit. brain:promote ALREADY does that. Two implementations of a written rule, one commit after it was written.\n\nTrap: compileAgentsMd(docs) keys by relPath and falls back to `?? ''` per section — passing an ARRAY silently emits empty sections. It deleted 543 lines and returned successfully.","issue":479,"source":"plainfiles save on vm"}
{"id":"rec-5d9a03da98881b61","ts":"2026-08-11T11:15:01Z","actor":"fix/issue-530-memory-capture-reachable","actorKind":"agent","type":"decision","project":"brain","content":"**Split paralelo: #530/#533 vs #535/#509/#435**\n\nParallel split, with the file overlap COMPUTED not asserted.\n\nAgent A (this session): #530, #533. Agent B: #535, #509, #435.\n\nClaims — #530: brain/scripts/memory/**, brain-save.mjs, session-start.mjs, .memory/**, package.json · #533: brain/scripts/vcs/providers/**, vcs/cli.mjs, brain/scripts/status/** · #535: .github/workflows/governance.yml, vcs/lib/workflow-auth.mjs · #509: brain-promote.mjs, harness/backends/** · #435: package.json, brain/project/decisions/**, docs/**.\n\nRun through filesOverlap (the function #459 shipped): exactly ONE pair overlaps — #435 ↔ #530, both on package.json. Small and mergeable, but whoever lands second REBASES rather than resolving blind. The #522/#523 lesson: a conflict resolution silently deleted four tests, and deleting tests never turns a suite red.\n\n#533's claim is deliberately narrow — vcs/providers/** and vcs/cli.mjs, NOT vcs/**. Claiming the tree would collide with #535 and serialise two tracks that need not be.\n\n#535 needs status:approved before work starts; the agent may never apply it (#124).","issue":313,"source":"plainfiles save on vm"}
{"id":"rec-b3e61c97c33e674f","ts":"2026-08-11T13:11:38Z","actor":"feat/issue-533-epic-map-executor","actorKind":"agent","type":"decision","project":"brain","content":"**Dos fuentes, un grafo: la unión se toma y la divergencia se reporta (#533)**\n\n**La pregunta que #533 pidió resolver era la precedencia. La respuesta es que no hay precedencia.**\n\n**Decisión**: el bloque declarado `brain-graph/1` y las relaciones nativas del proveedor (GitHub issue dependencies, GitLab issue links) son DOS FUENTES de un mismo grafo. Se toma la UNIÓN y se reporta cada desacuerdo. Ninguna pisa a la otra.\n\n**Por qué, y no es preferencia**: la precedencia es una forma de DESCARTAR una afirmación, y no hay terreno sobre el cual descartar ninguna de las dos — ambas son afirmaciones humanas sobre la misma relación, ninguna derivada de la otra. El grafo existe para responder «¿esto puede empezar ahora?», y para eso una arista que cualquiera de las fuentes conoce es una restricción real. Descartarla porque la otra fuente calla hace que el mapa diga «no hay dependencia», la afirmación más fuerte y más falsa — la misma que este módulo ya rechaza tres veces (el stub fuera-de-alcance en renderMermaid, la paginación de issueList, los commits no enumerados de #518).\n\nY la asimetría no es pareja: **un bloqueante de más demora un ticket; uno de menos habilita a dos agentes sobre trabajo que colisiona.**\n\n**El reporte no es decoración: sin él la unión es inauditable.** Es lo que responde la preocupación del propio ticket sobre «una relación que alguien clickeó por accidente» — un click errado aparece en una lista sobre la que un humano puede actuar, mientras que una arista pisada en silencio no aparece nunca. Una fusión silenciosa de dos fuentes es cómo un artefacto derivado vuelve a mentir.\n\n**Medido ANTES de diseñar (#335 — verde en test, inerte en producción)**: los tres endpoints de GitHub responden 200; `assignees` está presente en TODA entrada viva; y **0 de 47 issues abiertos declaran bloque y 0 llevan relación nativa**. Las dos fuentes están vacías hoy — así que la regla tenía que sobrevivir a que una fuente calle, que es el único estado en que se la ha observado. Después se corrió el flujo completo contra la API real sobre esos 47 issues.\n\n**Tres rechazos dentro de la unión**: un lado nativo ILEGIBLE no es «no tiene relaciones» (`null`, contado, y NO fabrica divergencias — «falta en nativo» no es un hecho sobre el dato cuando nativo no se pudo leer); las relaciones cross-repo se CUENTAN y nunca se dibujan (los números son por repo: un `#12` ajeno dibujado afirma una arista contra el `#12` de ESTE repo); y sub-issues + `relates_to` no se leen — contención no es orden, leer sub-issues como bloqueantes haría que cada slice de #313 pareciera bloquear a #313.\n\n**Lo que NO se decidió**: si el bloque declarado debería generarse desde las relaciones nativas o al revés. Hoy ambas son escritas a mano. Un generador haría a una de ellas derivada, lo que cambia la respuesta a «cuál gana» — una fuente derivada no tiene reclamo independiente — y es otro ADR cuando haya demanda.\n\nADR-0029, borrador bajo `brain-drafts/`: `brain/**` es Tier 2 y la firma es humana.","issue":533,"source":"plainfiles save on vm"}
{"id":"rec-ee9665678dbc7c32","ts":"2026-08-11T13:11:59Z","actor":"feat/issue-533-epic-map-executor","actorKind":"agent","type":"discovery","project":"brain","content":"**El puerto puede sobrescribir prosa humana, y la contención tuvo que existir primero (#533)**\n\n**`issueUpdate` es el primer verbo del puerto que puede SOBRESCRIBIR PROSA HUMANA.** Todos los demás escriben agregando: `issueComment` agrega un comentario, `labelAdd` agrega una etiqueta, `prReviewComment` publica una review. Este reemplaza, y no hay deshacer que el puerto pueda alcanzar.\n\n**#533 lo planteó como precondición, no como detalle a descubrir después**, y así se implementó: `replaceMapRegion` está acotado por marcadores por construcción, y `outsideRegion` prueba igualdad byte a byte de todo lo que rodea a los marcadores ANTES de que `brain:epic:map` escriba. El verbo recibe un string opaco y no puede verificarlo — exactamente por eso la prueba tenía que existir antes.\n\n**El modo de falla es real, no hipotético**: un cuerpo con un `BEGIN` huérfano y sin `END` — media región pegada a mano — hace que el append deje el marcador huérfano como el PRIMERO, así que la corrida siguiente se comería la prosa entre él y el `END` real. El mapa rechaza ese cuerpo y no escribe nada. Ese es el caso sobre el que se testea el guard.\n\n**`body` es el único campo escribible.** Un payload que además pudiera llevar `state`/`state_event` convertiría a este en el verbo que CIERRA un ticket, y cerrar es un acto de gobernanza: `issue-link` lee la palabra de cierre. Ensancharlo es un `decision`, no un parche.\n\n**Ausente no es vacío, en tres capas.** `assignees` es `null` cuando el payload no traía el campo y `[]` cuando traía uno vacío. `relations` es `null` cuando la lectura FALLÓ y `undefined` cuando nunca se pidió (`--no-relations`, o un proveedor sin el verbo). Tres estados, no dos — el tercero es el que una implementación equivoca por tener sólo dos, y por eso `--no-relations` se testea porque NO LLAMA a nada, no porque produzca un grafo más chico.\n\n**Diez mutaciones, nueve ROJAS. Una sobrevivió y una es equivalente.**\n\nLa que sobrevivió: el colapso `?? []` en la capa CLI no tenía cobertura. La regla estaba defendida donde se escribió (el puerto) y no donde se usa — #335 en miniatura. Se cerró con tres guards nuevos.\n\nLa equivalente CORRIGIÓ UN COMENTARIO MÍO: `??` contra `||` en la cadena de assignees no hace ninguna diferencia, porque `[]` es truthy en JavaScript. El comentario afirmaba que `??` era lo que protegía el caso vacío. Sobrevivir la mutación es cómo se encontró que la afirmación era falsa; se corrigió el comentario, no el código.\n\n**Un defecto de slice 1 que slice 2 hizo visible**: un nodo READY sin `files` produce un `conflictsWith` vacío, que se lee como «paralelizable probado». No probó nada. Las relaciones nativas no traen `files` en absoluto, así que dejó de ser raro — el resumen ahora los nombra en vez de dejar que un resultado vacío parezca uno limpio.","issue":533,"source":"plainfiles save on vm"}
{"id":"rec-00599b20655216d6","ts":"2026-08-11T13:58:34Z","actor":"fix/issue-516-doctrine-gate-claim","actorKind":"agent","type":"discovery","project":"brain","content":"**La doctrina prometía un gate que no dispara — y había un quinto sitio que nunca existió (#516)**\n\n**§1c le dice a un humano que enmienda un ADR firmado que omitir el marcador de `brain/HOME.md` «hace fallar el gate». No lo hace.** #510 (PR #515) le enseñó a `adrPresence` a distinguir un ADR AGREGADO de uno MODIFICADO — correcto, y desbloqueó toda la clase de PR #507 — y una enmienda modifica.\n\nEsa frase es load-bearing porque es lo que un humano lee mientras decide si el paso es salteable, y el camino de enmienda no tiene herramienta detrás: `brain:promote` lo rechaza por diseño (`brain-promote.mjs:335`) y #509 no está construido. Las otras dos redes tampoco cierran: `brain:nav` pasa porque `HOME.md` ya linkea el ADR — lo que falta es el MARCADOR, no el link — y `phase-order` es detección en `lite`. Protección aparente, la clase de #499.\n\n**HAY CINCO SITIOS, NO CUATRO, y el quinto es el peor.** `workflow-governance.md` describe un `decision-gate` de DOS PASOS y CONDICIONAL A LA ETIQUETA — paso 1 duro «si el PR lleva la etiqueta `decision`», paso 2 una heurística que escanea superficies arquitectónicas y avisa. **Ninguno de los dos existió nunca**: `adrPresence` toma dos listas de archivos, ningún call site pasa etiquetas, el job del workflow no tiene condición, nada escanea ninguna superficie. Y ese archivo compila a `AGENTS.md`, así que la ficción está en las instrucciones que lee el propio agente. Lo encontré midiendo, no leyendo el ticket.\n\n**Rancio y aspiracional son defectos distintos que se leen igual.** Los sitios 1-4 describen comportamiento que ALGUNA VEZ fue cierto y se reescriben; el quinto describe comportamiento que NUNCA lo fue y se MARCA como no implementado — retirarlo en silencio descartaría una intención de diseño que nadie decidió abandonar.\n\n**Lo que deja atrás un arreglo de prosa, cuando la afirmación es sobre código.** La mitad de código ya estaba bien anclada (los tests de #510). La afirmación de la DOCTRINA no tenía ancla ninguna — por eso las dos podían divergir mientras nadie leyera ambas. Dos tests ahora afirman los hechos de código que la prosa describe (el veredicto es idéntico con y sin la etiqueta `decision`; un cambio arquitectónico sin ADR simplemente pasa) y sus mensajes de fallo NOMBRAN los archivos de doctrina que tienen que moverse en el mismo cambio. Cada uno probado detector real por una mutación que IMPLEMENTA la afirmación de la doctrina — condicionalidad por etiqueta: 4 rojos; la heurística: 1 rojo.\n\n**La nota de divergencia de ADR-0026 no se podía retirar y ya.** Registra DOS divergencias en una oración. La mitad agregado/modificado cierra acá; la mitad de ETIQUETAS sigue abierta, porque la fila `standard` todavía promete un paso duro por etiqueta `decision` que nunca se envió. Partir la nota era la única edición honesta.\n\n**Las dos ramas que fallan del gate están keyeadas distinto, y eso ES el contenido de #510**: la rama A lee la lista AGREGADA («un ADR agregado necesita entrada en `HOME.md`»), la rama B lee la TOCADA («un cambio a `HOME.md` necesita algún ADR»). Describirlas como una regla simétrica es exactamente cómo la doctrina se equivocó.\n\n**Este PR es una instancia del hueco que documenta**: enmienda un ADR firmado a mano porque `brain:promote` rechaza ese camino, y nada más que el humano atraparía un marcador omitido. El script de promoción se EJECUTÓ y se revirtió antes de shippear — cinco sitios en cuatro archivos más `AGENTS.md` regenerado, `brain:nav` verde, el drift guard verde, y una segunda corrida reportando «already promoted». La checklist a mano de #529 describió una edición de dos archivos para una cascada de tres y CI la agarró sobre el commit de firma; esta es una ruta que ya se caminó.","issue":516,"source":"plainfiles save on vm"}
{"id":"rec-bf243645c13a32a7","ts":"2026-08-11T15:00:54Z","actor":"fix/issue-340-local-ci-parity","actorKind":"agent","type":"discovery","project":"brain","content":"**brain:check greenlighteaba PRs que CI rechazaba — y la auditoría encontró tres, no una (#340)**\n\n**`brain:check` es el verbo que el golden path te dice que confíes antes de shippear, y reportaba «6/6 pass, Ready to brain:ship» sobre PRs cuyo job `issue-link` después fallaba.** Una regla, dos implementaciones, nada atándolas: `brain:check` llamaba a las funciones PURAS mientras CI llama a los evaluadores que aplican política encima — la regla de rama base, la verificación de la etiqueta aprobada, el match de memoria scopeado al issue.\n\n**La dirección es lo que lo hace defecto y no molestia**, y el ticket lo dice: un check local más estricto que CI es molesto; uno más laxo es una promesa rota.\n\n**LA AUDITORÍA ENCONTRÓ TRES, NO UNA.** La última línea de #340 — «auditá los otros cinco antes de asumir que este está aislado» — era la instrucción load-bearing. `Part of #N` a la rama por defecto pasaba local y fallaba en CI; también una referencia de cierre a un issue NO APROBADO; también un `session_summary` scopeado a un issue distinto del que se cierra. **Dos de las tres eran invisibles al ticket**, porque la verificación de etiqueta y el match scopeado son capas de política que CI creció DESPUÉS de que `brain:check` fuera escrito.\n\n**El arreglo no es alinear regexes ni una segunda implementación mantenida en sync**: `brain-check.mjs` importa `runCheck` de `governance/run-check.mjs`, la MISMA función que invoca el job de CI. No queda nada que mantener en sync porque no queda nada con qué estar en sync.\n\n`diffSize` y `adrPresence` se quedan en las funciones puras a propósito, y la auditoría es el motivo: `adrPresence` está alineado por construcción desde #510; `diffSize` diverge sólo en la dirección SEGURA — CI honra `size:exception`, no existe etiqueta antes de que exista el PR, y ruteárlo por el evaluador significaría INVENTAR un set de etiquetas, el único cambio que podría hacer local más laxo.\n\n**UN TERCER ESTADO.** Evidencia que no se pudo juntar es `[UNVERIFIED]`, nunca `PASS`, y el reclamo «Ready to brain:ship» queda suprimido mientras haya alguno. No es exit-1 a propósito: CI falla cerrado porque hay un merge en juego, mientras que un verbo local que se niega a correr offline es un verbo que la gente deja de correr (la lección de #529). **El defecto nunca fue «local pasó» — fue «local AFIRMÓ»**, y sacar la afirmación es lo que lo elimina.\n\n**`init.defaultBranch` se probó y se SACÓ**: describe ramas que git CREA, no la de este remoto, así que leerlo como fallback responde con confianza un valor sin relación con el repo — un default fabricado justo en el lugar donde el ticket dice fallar cerrado. La rama TARGET no se puede saber localmente en absoluto (el PR no existe todavía), así que default a la regla más estricta con `BASE_BRANCH` como opt-out explícito.\n\n**Un fixture de test codificaba el defecto**: el contexto «all checks pass» de `brain-check.test.mjs` combinaba un `session_summary` sin `issue` con un body `Closes #42` — combinación que CI rechaza. Afirmaba exit 0 sobre un cambio que CI falla. Se corrigió SCOPEANDO el summary, no relajando la aserción.\n\n**El suite de paridad es lo que hace que no se repita**: siete fixtures, cada uno describiendo un cambio UNA vez, alimentados a las dos superficies; primero unilateral (local nunca pasa donde CI falla) y después igual sobre evidencia idéntica, porque un check local hardcodeado a fallar satisfaría la regla unilateral y sería inútil. Tres mutaciones rojas: las llamadas puras pre-#340 → 5, asumir slice → 1, renderizar uncomputable como pass → 2.\n\n**Hazard 2 archivado como #545**: `extractIssueNumber` es first-match-wins, así que «deliberately does NOT close #405» seguido de «Closes #485» hace que brain verifique la aprobación sobre **#405**. Es otro defecto — este ticket es dos implementaciones de una regla; aquel es una implementación leyendo mal su input.","issue":340,"source":"plainfiles save on vm"}
{"id":"rec-897af5f4dbb254b6","ts":"2026-08-11T16:08:31Z","actor":"feat/issue-535-vcs-token-gate","actorKind":"agent","type":"session_summary","project":"brain","content":"**issue-535 chain closed: VCS_TOKEN PR-time governance gate, T7b residual deferred to #551**\n\nWhat shipped: issue #535 — the PR-time governance gate authenticated through the GitHub-only GH_TOKEN. It worked only by accident: with no VCS_TOKEN set, nothing binds and `gh` picks up the ambient GH_TOKEN, so a GitLab consumer would have received six required jobs wired to a credential their provider never reads. GH_TOKEN is now fully removed from the three port-reaching steps, never left alongside VCS_TOKEN.\n\nThe ticket's premise was partly wrong: `issue-link` (governance.yml:54) had ALREADY been fixed by #479, so three steps needed the GH_TOKEN → VCS_TOKEN swap, not four. The measurement corrected the ticket during implementation.\n\nSecond half of the work: `auditWorkflowAuth` now resolves port-reach PER INVOCATION rather than per file, via a SUBCOMMAND_PORT_REACH manifest declared in run-check.mjs and read as TEXT — never imported, because importing it would make the guard the first violator of the \"run-check.mjs is an entry point, never a library\" invariant it enforces. governance.yml joined the audited workflow set. The permissions: regex used to match only flow style while governance.yml uses block style, so condition 2 was silently unevaluated — fixed. A further residual closed in PR D (#550): permissionsScopes() also collapsed \"permissions: header present but zero scope lines\" (a block of only comments, or flow-style `permissions: {}`) into the same null as \"no permissions: key at all\", silently skipping the pull-requests-scope check for the most restrictive possible grant — now returns '' (present, empty) to stay distinguishable.\n\nReal-run evidence (the #475 bar): actor-check, brain-writes-reviewed, and issue-link all went green on the chain's own PR runs, authenticating through VCS_TOKEN with no GH_TOKEN present. Note explicitly that diff-size going green is NOT evidence — loadGithubContext swallows prView failures (ci-context.mjs:74) and leaves labels = null, so a small diff passes even with a broken credential.\n\nDelivery: 861 changed lines against a 400-line lite budget, shipped as a feature-branch chain of four PRs, each under budget, cut at prefix boundaries with no commit reordering (reordering conflicts in workflow-auth.test.mjs). Suite verified green at every cut point: 3096 / 3108 / 3119, and PR D's own cut point at 3122 (69+6=75 changed lines).\n\nReview: sdd-verify returned PASS WITH WARNINGS (both closed). Then three rounds of dual blind adversarial review found, in sequence: a fail-open where a YAML comment as the first line under block-style permissions: silently disabled the pull-requests check; a new FALSE caveat in the very comment block this change rewrote (it claimed a corrupt manifest resolves to unresolvable when it actually falls back to the D2 whole-file rule); then two independent vacuous-pass routes in the test (T7b, in run-check.test.mjs) written to close the previous round's finding; then a THIRD route in the same test.\n\nThe lesson worth carrying: every one of those three T7b routes is the same defect — the test conflates \"could not compute a closure\" with \"computed a closure and found nothing.\" Extending the regex closed routes 1 and 2 and left route 3 open (an arrow-function or function-expression dispatch handler still falls through bodyClosure and crossFileClosure to an empty '' closure, which trivially agrees with a manifest entry already declared false). The structural fix is fail-loud on a non-computable closure, matching the production guard's own fail-closed polarity, rather than extending the regex a fourth time. Recorded as open follow-up issue #551 (\"test(governance): T7b agrees vacuously when a dispatch handler is an arrow function — the third route to the same empty-closure defect\"), referenced from PR D (#550).\n\nOpen state for the next agent: the chain is PR A #547 (base: tracker feat/issue-535-vcs-token-gate) → PR B #548 (base: PR A's branch feat/issue-535-01-swap-and-foundation) → PR C #549 (base: PR B's branch feat/issue-535-02-per-invocation) → PR D #550 (base: PR C's branch feat/issue-535-03-guard-coverage), all open. The tracker PR targets main, is a DRAFT, and its body contains \"Closes #535\" — it is the only PR in the chain that targets the default branch. main advanced 4 commits (through #543/#544/#546-equivalent merges) after the chain was branched from eb8810d, so the tracker will need a rebase before it can merge. The merge decision belongs to the maintainer; nothing in this chain should be merged autonomously.\n\nProcess note: two blind judges were run mutating the SAME worktree concurrently during the round-3 review and interfered with each other's mutation-proof state; both compensated for it, but future dual reviews need one worktree per judge to avoid this class of interference.","issue":535,"source":"plainfiles save on PCStaFe30-174"}
{"id":"rec-b70d33d3d22e62d2","ts":"2026-08-11T21:40:18Z","actor":"feat/issue-535-vcs-token-gate-landing","actorKind":"agent","type":"session_summary","project":"brain","content":"**issue-535 landed on main: the Requirement 6 / #340 collision the textual merge hid**\n\n**Context.** The #535 chain (PRs #547/#548/#549/#550, four stacked branches on a tracker branch that had no PR to main) was complete and green, but based at `eb8810d` — before #546 (#340) and #553/#554 merged. Landing it meant replaying its 15 commits onto current main, which is where the real finding surfaced.\n\n**The finding: two merged-doctrine requirements that only collide in the blanket form.** #535's Requirement 6 shipped a test forbidding ANY non-test module from importing `governance/run-check.mjs` (\"entry point, never a library\"). #340 (merged as #546, AFTER the chain branched) makes `brain-check.mjs:27` import `runCheck` on purpose — brain:check and CI must be ONE implementation of one rule. `git merge-tree` reports the merge clean; the test suite reports it red. Textual mergeability said nothing about semantic mergeability, and each side was written blind to the other because both were in flight at once.\n\n**Resolution, and why not the other one.** The property per-invocation resolution actually needs (workflow-auth.mjs, Requirement 3) is narrower than the ban: no OTHER WORKFLOW-INVOKED entry point may reach run-check.mjs, because such a step carries no SUBCOMMAND_PORT_REACH manifest of its own and the guard therefore resolves it by whole-file closure, inheriting every subcommand's port-reach at once. `brain-check.mjs` is a local developer verb invoked by no workflow, so it cannot cost any CI step its precision. Narrowing keeps both requirements; reverting #340's import would re-create the defect #340 exists to remove. The narrowed test goes red the day anything reaching run-check.mjs becomes a workflow step — which is exactly when the extraction has to happen.\n\n**How the narrowing avoided becoming its own defect.** The set of workflow-invoked entry points is derived through workflow-auth's OWN extraction (new export `workflowEntryPoints`) and its OWN closure walk (`importClosure`, now exported) — a second copy of either would be #340 a third time. Not an allowlist of file paths: that is the shape of the guard #480 defeated. `importClosure` stops at `vcs/ci-context.mjs` (the D1 cut vertex), which would hide a path to run-check.mjs through it, so that gets its own direct canary instead of an assumption.\n\n**Red-proof, run live, not reasoned.** Appended a real `import { runCheck } …` to `brain/scripts/vcs/actor-check.mjs` (a genuine workflow entry point), confirmed the edit landed by reading the file back, ran the suite: RED with the intended message. Reverted from a pre-mutation snapshot, confirmed clean, green. A synthetic-file mutation test pins the walk itself so the invariant cannot start passing by seeing nothing.\n\n**Corrected premise carried forward from the chain's own record.** #535's table claimed four steps declared GH_TOKEN; `issue-link` had already been moved to VCS_TOKEN by #479 (commit `316575a`), so the swap covered three. Verified on `origin/main` before landing, not taken from the ticket.\n\n**State on landing.** 3174 tests, 0 fail; `repo:check` and `brain:nav` green. Non-test changed lines ~381 against the lite budget, well inside it — the chain was split into four PRs against a 400-line-per-PR working limit, but the tests are in `ignoreList`, so the whole thing lands as one PR without a waiver. Deferred, deliberately and at the maintainer's instruction: #551 (T7b's third vacuous-pass route — arrow-form dispatch handlers; test-coverage only, no production defect).\n\n**Generalizable.** When two branches are in flight against the same subsystem, \"merges clean\" is not the parallelisability test — the test is whether either side introduces a STANDING INVARIANT the other side violates. An invariant is a claim about the whole tree; a diff is a claim about a few files. #459's `filesOverlap` compares file claims and would have called these two disjoint (`brain-check.mjs` + `governance/run-check.mjs` vs `workflow-auth.mjs` + `governance.yml`) — it cannot see an invariant whose scope is \"every file\". That is a real limit of the parallelisation signal, worth naming where the signal is documented.","source":"plainfiles save on vm"}
{"id":"rec-e33c86b9ead9d9a2","ts":"2026-08-11T22:09:33Z","actor":"feat/issue-535-vcs-token-gate-landing","actorKind":"agent","type":"session_summary","project":"brain","content":"**issue-535 cold review: the narrowing was wrong, and the test I deleted was catching more than I measured**\n\n**Two independent cold reviewers, run in parallel on #556 in separate git worktrees, converged on the same blocker with different mutations.** Recording it because the mistake is reusable, not because the fix is interesting.\n\n**What I did.** Landing the #535 chain on `main` turned the suite red on Requirement 6's blanket ban (\"no non-test module imports `governance/run-check.mjs`\"), because #546 (#340) had merged in the meantime and makes `brain-check.mjs` import `runCheck` on purpose. I replaced the blanket ban with a REACHABILITY computation: derive the CI-invoked entry points from `.github/workflows`, walk their import closures through `workflow-auth`'s own `importClosure`, assert none reaches `run-check.mjs`. It looked strictly better — mechanically derived, no allowlist, reusing the guard's own machinery rather than a second copy — and it passed a live red-proof (a static import injected into `actor-check.mjs`, a real workflow entry point, went red).\n\n**What both reviewers proved, independently.** The reachability form under-approximates at three separate places, and every under-approximation resolves toward \"fine\":\n\n1. `importClosure` prunes AT `vcs/ci-context.mjs` (the D1 cut vertex) without walking its imports. Reviewer A injected `ci-context.mjs -> ci-context-helper.mjs -> run-check.mjs`; reviewer B injected `ci-context.mjs -> gitlab-api.mjs -> run-check.mjs` (an import that already exists). Full suite GREEN in both cases. Four workflow entry points carry `ci-context.mjs` in their closure, so all four would genuinely reach the multiplexer with the invariant green. **The canary I wrote for exactly this hole — \"`ci-context.mjs` does not import `run-check.mjs`\" — covers one arc of it, not the hole**, and my header claimed it closed the hole. That claim was false.\n2. The extraction reads `.github/workflows` only. This repo ALSO ships `brain/scripts/ci/gitlab-governance.yml`, which invokes the same seven entry points with `script:` lists (ADR-0018, dogfooded, and the surface #130 depends on). Never scanned, and `runScript` only recognizes `run:` so it could not have parsed it anyway. My docstring said \"every entry point every workflow in the repo invokes\" — false as written.\n3. `entryPoints` resolves `node <path>.mjs` and `npm run <verb>`, nothing else — not `npm test` (already used at `governance.yml:123`), not `bash wrapper.sh` (the repo's own `npm run env:init` idiom), not `npx`, not a quoted path. Each unrecognised spelling made the invariant agree with anything on that route. My red-proof used the ONE spelling the extractor handles: `red-proof-blind-along-an-unvaried-axis`, where the unvaried axis was in the EXTRACTOR, not in the assertion.\n\n**The decisive measurement**: reviewer A re-implemented the deleted blanket predicate against the mutated tree and it flagged the offender. The test I removed caught the case my replacement missed. A narrowing justified as \"keeping the property while dropping the over-approximation\" had in fact dropped a chunk of the property.\n\n**What replaced it.** Back to the blanket source-text ban — no cut vertex, no YAML parsing, no spelling dependence — plus ONE frozen exception (`brain-check.mjs`), asserted by EQUALITY so a second importer and a silently-dropped one are both red. The exception is safe for exactly one reason (no CI surface invokes `brain-check`), and that reason is a fact that can change silently, so it is asserted too: a raw substring scan for `brain-check|brain:check` over every CI file across BOTH surfaces (`.github/workflows/**` and `brain/scripts/ci/**`), comments included. Deliberately over-approximating — a mention costs a false alarm and one line of thought; a missed invocation costs the invariant. The import predicate now also matches the dynamic `import()` spelling, which the original blanket test missed too. Seven mutations, each snapshot -> edit -> read back -> run -> restore -> verify clean: every case both reviewers proved green is now red, including the frozen set's other direction.\n\n**Generalizable, and the real lesson.** A guard has a polarity, and a REFINEMENT inherits the polarity of its weakest step. Blunt-and-over-approximating fails toward a false alarm; precise-and-computed fails toward silence, because every stage that cannot resolve its input contributes a \"nothing here\". When the choice is between a blunt rule with a named exception and a precise rule with a derivation, prefer the blunt one unless every stage of the derivation is itself fail-closed — and the way to find out is to mutate BEHIND each stage, not in front of it. My red-proof injected the defect where the walk was looking. Both reviewers injected it where the walk stops. That is the difference between proving a test can go red and proving it observes the thing it names.\n\n**Corrections to two earlier records in this month's file.** Records are content-addressed and immutable — an in-place edit is caught by `rebuildIndex` as tampering (verified: it refused), so these are corrections by append, not by edit.\n- `rec-897af5f4dbb254b6` (the chain closure record) says \"861 changed lines against a 400-line lite budget\". `tierParams('lite').diffBudget` is **1000**; 400 is the `standard`-tier literal quoted in `governance.yml` and the chain's self-imposed per-PR working limit.\n- The same record lists `brain-writes-reviewed` going green as real-run VCS_TOKEN evidence. It is not: the job passed on rule 1 before any port call (this PR touches no `brain/core/**` or `brain/project/**` file), and at `lite` the review fetch is skipped outright (`brain-writes-reviewed.mjs:357`). The genuine evidence is `issue-link`, which fails closed on both an uncomputable body and an `issueView` failure, so green requires two successful authenticated calls. `actor-check`'s LOG is evidence; its green alone is not (`!author -> warn -> exit 0`).\n- `rec-b70d33d3d22e62d2` (my landing record) says \"Non-test changed lines ~381\". The measured figure is **411** (327 additions, 84 deletions, excluding the ignoreList), which is what the PR body states.\n\n**Filed out of scope, not fixed here**: `governance-relabel.yml` carries the exact #535 defect (`GH_TOKEN` on a step whose entry point imports `getVcs`) and escapes the guard because the audited set is a hand-written three-element list; `workflow-auth` answers \"nothing to report\" for `node \"quoted/path.mjs\"`; the audit only ever reports need>declared, so over-credentialing a credential-free job is invisible, and its `permissions:` check covers `pull-requests` but not `issues: read`, which `issue-link` needs.","source":"plainfiles save on vm"}
{"id":"rec-29c213520b4af89e","ts":"2026-08-11T22:25:46Z","actor":"feat/issue-509-promote-amendments","actorKind":"agent","type":"session_summary","project":"brain","content":"**issue #509 — brain:promote slice 2: in-place amendment promotion, byte-identical to be2d143**\n\n**Actor:** claude-opus-5 (agente)\n**Fuente:** issue #509 / branch feat/issue-509-promote-amendments\n\nWHAT SHIPPED\n`brain:promote` now takes a second draft shape: `*.draft.md` + one fenced `brain-amendment/1`\nblock = an in-place amendment to an already-signed brain/** file. New pure module\n`brain/scripts/lib/amendment-draft.mjs` (contract parser + total planner); `runPromote` dispatches\nbetween two planners and keeps ONE copy of each ADR-0028 lock. `promote-529.sh` and\n`promote-516.sh` deleted; a test keeps them deleted.\n\nTHE CONTRACT (the design question the ticket asked to settle)\nThe draft declares CONTENT, the verb generates SHAPE. Scalars: target, amendment, issue,\nhome-summary, body, body-end. §1c act 2 = ordered `amend-find`/`amend-replace` fence pairs\n(exact strings, refuse on any count != 1 — both stopgaps' property, kept). The verb generates the\nStatus line (and VERIFIES the amendment number is target's+1), the DD/MM/YYYY stamps, the\n`**Signed**:` line, the brain/HOME.md marker, and the AGENTS.md regeneration. `.draft.md` is\nREQUIRED in addition to the contract block, so a doc that merely quotes a contract is never\npromotable. Unknown contract keys refuse.\n\nTHE ORACLE\n`be2d143` (ADR-0026 Amendment 2, promoted by hand). Test:\nbrain/scripts/brain-promote.golden.test.mjs — builds a repo from the tree at be2d143^, assembles\nthe draft from the pre-existing #473 draft (prose + act-2 annotation) plus the PRE target (the\nanchor), runs the verb, asserts the ADR file, brain/HOME.md and AGENTS.md are BYTE-IDENTICAL to\nbe2d143, zero commits, three staged paths, and the printed commit command passes the real\ncommit-msg hook. Verified: the #473 draft's signed section is already byte-identical to what\nbe2d143 appended once `**Signed**:` is stamped.\n\nRED-PROOF (6 mutations, each read back off disk before running)\nM1 act 2 skipped -> RED; M2 act 1 status line computed-not-applied -> RED; M3 HOME.md marker not\nwritten -> RED; M4 AGENTS.md regeneration skipped -> RED; M5 ISO instead of DD/MM/YYYY -> RED;\nM6 non-literal String.replace ($& expands) -> RED. All restored, md5 verified.\n\nTHINGS FOUND THAT ARE NOT IN THE TICKET\n1. The #473 draft's act-2 paste block is LINE-WRAPPED prose; the ADR carries that annotation on\n   ONE line inside a table row. The human unwrapped it by hand. A prose draft could not have been\n   applied verbatim by any tool — that is the concrete case for a machine-readable contract.\n2. Idempotence needs the STOPGAPS' key, not an anchor count: an anchor that is a PREFIX of its own\n   replacement (\"## Lockout Recovery\" -> \"## Lockout Recovery (amended ...)\") still occurs exactly\n   once after the edit lands, so an anchor-count check alone double-applies. Found by a real\n   double-apply. The check is \"every declared replacement already present\".\n3. compileAgentsMd's `?? ''` fail-open now THROWS on any missing SOURCE_DOCS key (array included).\n   init() never relied on it. A test shows why it was invisible: the gutted output has the same\n   banner and the same five section headers as a good one.\n4. AGENTS.md is always recompiled but only WRITTEN when it differs — a target outside the five\n   SOURCE_DOCS compiles to identical bytes, and staging a no-op reports a path the human cannot\n   find in `git diff --cached`.\n\nWHAT IS DELIBERATELY NOT HERE\nNo gate. #516's content-keyed amendment-marker check is buildable and unbuilt — the verb is the\nnet only for promotions that go through the verb; a hand-run amendment is exactly as unguarded as\nbefore (design D5). No commit, no push. One target per run.\n\nTIER 2 — NOT PROMOTED\nTwo amendment drafts under openspec/changes/issue-509-promote-amendments/brain-drafts/, both\nwritten in the new contract format and both DRY-RUN against a throwaway copy of the real tree:\nadr-0028-amendment-1.draft.md (ADR-0028's slice-1 scope paragraph is now stale) and\nconsolidation-protocol-amendment.draft.md (§1c/§1d say \"until #509 ships\"). The human promotes\nthem by running `npm run brain:promote -- <draft>`; their commit is the signature.\n\nRESIDUAL\nThe golden oracle lives in git history and CI's local-checks checks out at depth 1. The suite\ntries one bounded `git fetch --depth=2 origin be2d143`, then reports SKIP with the exact command\n— never a pass. It ran live here (13/13). Counted diff: 990 of the lite budget's 1000.","source":"plainfiles save on vm"}
{"id":"rec-b0667de66f121973","ts":"2026-08-11T22:37:22Z","actor":"feat/issue-509-promote-amendments","actorKind":"agent","type":"session_summary","project":"brain","content":"**issue #509 addendum — the golden suite was SKIPPING in CI; abbreviated sha is read as a ref name**\n\n**Actor:** claude-opus-5 (agente)\n**Fuente:** issue #509 / PR #562\n**Supersede:** the \"RESIDUAL\" paragraph of the earlier #509 record (rec saved same session) — the\nskip is no longer the CI path.\n\nThe first CI run on PR #562 was all green and reported `# skipped 13`: the ENTIRE\nbrain-promote.golden.test.mjs suite — the acceptance test for the ticket — never executed. A\nfull checkout skips 0 (the one pre-existing skip, copyManaged, does not trigger off-root).\n\nCAUSE: CI's local-checks job checks out at depth 1, so the oracle commit be2d143 is absent, and\nthe suite's fallback `git fetch --no-tags --depth=2 origin be2d143` FAILS with\n\"fatal: couldn't find remote ref be2d143\". An ABBREVIATED sha is read as a REF NAME by git fetch;\nit is not resolved as an object. The full 40-char sha works and takes ~1s:\n`git fetch --no-tags --depth=2 origin be2d143e6cb62748369ba2f1a11425845eeeebaf`.\n\nMEASURED, not reasoned: `git clone --depth 1 --branch <branch> https://github.com/csrinaldi/brain`\ninto a temp dir, confirmed `git cat-file -e be2d143^{commit}` fails there, then ran the suite —\n13/13 pass with the full sha, 13/13 skip with the abbreviation. CI after the fix: 3262 pass,\n0 fail, 0 skipped.\n\nGENERAL LESSON, worth more than the bug: a skipped test and a passing test are the SAME COLOUR on\nthe PR page and in the check-run conclusion. The only signal that distinguished them was the\n`# skipped` count in the job log. Any test that guards its own environment with `{ skip: ... }`\nneeds its skip count read at least once in the environment it is supposed to protect — otherwise\n\"CI is green\" means \"CI ran nothing\", which is this repo's own\nevidence-reader-empty-on-failure / red-proof-blind anti-pattern at the harness level.","source":"plainfiles save on vm"}
{"id":"rec-edfbd4b3f058ba25","ts":"2026-08-12T01:35:06Z","actor":"feat/issue-509-promote-amendments-02","actorKind":"agent","type":"session_summary","project":"brain","content":"**issue #509 review round — the cascade is one unit, and git add is not inert**\n\n**Actor:** claude-opus-5 (agente)\n**Fuente:** issue #509 / PRs #562 (A) + follow-up (B)\n**Supersede:** the idempotence paragraph of the earlier #509 record — \"every declared replacement\nis already present\" was BOTH the wrong key and only one of three exits.\n\nTwo cold reviewers, disjoint halves, isolated worktrees, no context on the authoring session.\nFour blockers. All fixed; every fix red-proved by a mutation read back off disk.\n\nTHE CENTRAL DEFECT (BLOCKER 1). `alreadyPromoted` had THREE independent exits — all replacements\npresent, the body heading alone, the HOME marker alone. Exit 2 fires in the NORMAL case: the human\npastes the signed section by hand (how 0f54781, f04ec5a and be2d143 were all actually done), runs\nthe verb to finish the cascade, and gets \"✓ already promoted\" with acts 1, 2 and 4 undone —\nincluding the brain/HOME.md marker that has no gate behind it (#516). Deleting the branch left all\nthree suites green: nothing pinned it either way. promote-516.sh keyed on the LAST act of the\ncascade; the verb keyed on the first. Reading one act to conclude something about four is a guess.\nNow: assessCascade reads every act, three dispositions (all pending → apply, all done → check\nAGENTS.md too, mixed → REFUSE naming each act's state).\n\nTHE KEY (BLOCKER 2). `includes(replace)` also refuses a GENUINE first application whenever the\nreplacement text legitimately appears elsewhere in the document. Settled on counting FREE anchors:\nfree = f − r×k where f=count(target,find), r=count(target,replace), k=count(replace,find).\ndone ⇔ r≥1 ∧ free=0; pending ⇔ free=1; blocked otherwise. One tie is NOT breakable by counting\n(one applied replacement + one free anchor is arithmetically identical to a quoted replacement +\nan unamended anchor: both f=2,r=1,k=1) — resolved to pending on purpose, because the alternative\nis blocker 2. Counting must include SELF-OVERLAPPING matches: String.split says `|---|---|` holds\none `|---|`, it holds two, and a Markdown separator row is a plausible anchor.\n\ngit add IS NOT INERT (BLOCKER 3). On an unmerged path it marks the conflict RESOLVED. Mid-rebase\nthe verb staged <<<<<<< markers into brain/HOME.md and into the AGENTS.md compiled from it, exit 0,\n\"✓\". Lock 3's allowlist never made \"stages, never commits\" true by itself. Now: repo-wide unmerged\nrefusal (repo-wide because AGENTS.md compiles from five source docs), `status` added to the\nallowlist, and the lock's property re-asserted by NAME (commit/push/tag/reset/... excluded) rather\nthan by the list's length.\n\nA GREEN SUITE THAT ASSERTED NOTHING (BLOCKER 4), round two. The full-sha fetch fixed one CAUSE;\nthe CLASS survived — every golden test carried {skip} and node --test exits 0 on skips. Shallow\nclone + unreachable origin = EXIT 0, pass 0, skipped 13. Now one UNSKIPPED test asserts the oracle\nis reachable: same setup now exits 1. Verified by reproduction, not by reasoning.\n\nALSO: writes had no rollback (a failure on file 2 of 3 left a half-amended signed ADR and nothing\nstaged — and blocker 1 then reported success over it); a symlinked target wrote OUTSIDE the repo\nunstaged (containment was lexical, writeFileSync follows symlinks — now realpath + lstat);\nlock 2's structural guard scanned one file while the verb spans two (an env read in the new module\nleft the locks suite green and forged the **Signed** attribution — the guard now derives its file\nlist from the verb's own imports and fails on an unclassified one); staged-only edits to a write\npath were destroyed with no reflog (now refused); unstaged edits were staged invisibly (now\ndisclosed above the prompt); one stray CRLF rewrote every line (split/join → single-line splice).\n\nPROCESS LESSONS WORTH KEEPING\n1. A test that returns early because the environment cannot host the failure (chmod as root) is a\n   false green. Prove the capability, or inject the failure through a seam AND keep a real-FS test.\n2. Two of my own PR-body claims were false: \"which is what both scripts actually tested for\"\n   (promote-516.sh keyed on the HOME marker, not on replacements — I adopted the weaker key while\n   claiming they agreed) and \"no brain/** file is touched\" (seven are; all of brain/ is Tier 2,\n   only brain/core|project are Tier 3). A reviewer sizes L6 review from that sentence.\n3. 1423 counted lines forced a split. Cut at the module boundary — the same line the two reviewers\n   split along: A = pure algebra (blockers 1,2), B = I/O against a real repo (blockers 3,5,6,8).","source":"plainfiles save on vm"}
{"id":"rec-2bb3fcc9c4c76c5c","ts":"2026-08-11T22:21:37Z","actor":"feat/issue-558-audited-set-derived","actorKind":"agent","type":"session_summary","project":"brain","content":"**issue-558: the audited set was an allowlist, and the ticket's own proposal was wrong about GitLab**\n\n**The live defect.** `.github/workflows/governance-relabel.yml` declared `GH_TOKEN` on the step invoking `relabel-retrigger.mjs`, which imports `getVcs` and reaches the VCS port — the exact coupling #479 removed from the audit path and #535 from the PR-time gate. It survived both because `auditWorkflowAuth` was only ever run against a hand-written three-element list (`release.yml`, `governance-postmerge.yml`, `governance.yml`) in `release-postmerge-workflows.test.mjs`. `governance-relabel.yml` and `m4-danger-paths.yml` were outside it. Adding a workflow to the repo did not add it to the audit.\n\nThat is the guard's own anti-pattern one level up: a checker whose COVERAGE is an allowlist, where absence from the list reads as \"nothing to report\". #480 built the guard specifically so that a spelling it cannot recognise is a violation rather than a pass — and then the set of files it was pointed at was a literal someone had to remember to extend. Found by two cold reviewers of PR #556, independently, both while asking \"what does this guard actually cover\" rather than \"is this guard correct\".\n\n**Fixed:** the audited set is now derived from `.github/workflows/**` on disk, with a named floor (release/postmerge/governance/relabel must be present) so a rename or deletion is a failure rather than a quietly smaller set. Red-proved three ways on the real tree: the fix reverted → red; a brand-new non-compliant workflow written to the real directory, named in no list → red; a known workflow removed → red.\n\n**THE TICKET WAS WRONG, and measuring is what caught it.** #558 (which I wrote) proposed: \"teach the step reader GitLab's `script:` list, so the audit is one implementation across both providers rather than a GitHub-only guard plus an unexamined fragment.\" Measured, that produces SEVEN FALSE ALARMS on a correct file. GitLab injects CI/CD variables configured in project settings into every job's environment automatically — the fragment's own header says so, and it declares no `variables:` block at all. `auditWorkflowAuth`'s central rule, \"a port-reaching step must DECLARE the credential in its own `env:`\", is a GITHUB ACTIONS rule, because GitHub Actions is the provider requiring explicit per-step mapping. Applying it across providers would have flagged every port-reaching GitLab job for not declaring a credential that is legitimately ambient there. A guard that cries wolf is a guard someone switches off (#535's own words).\n\nSo the GitLab fragment gets `auditGitlabFragment`, a sibling audit with the rules that DO apply to that provider: no `GH_TOKEN`/`GITHUB_TOKEN` literal (the #479/#535 coupling in its purest form), and every entry point it invokes must exist — the failure mode that file is most exposed to, since no CI of brain's own ever runs it, so a renamed script breaks it silently and stays broken until a consumer hits it. Plus the vacuous-pass guard: a parse that finds no `script:` list at all is a violation, not a clean bill. All three teeth proved by mutation.\n\n**Evidence discipline — a green job here is NOT evidence, and the log line IS.** `relabel-retrigger.mjs` is documented \"never throws — best-effort re-trigger, not a REQUIRED gate\": both a `getVcs` failure and a PR-discovery failure log a `::warning::` and RETURN, and `main()` exits 0 regardless. So the job is green whether the credential works or not. What distinguishes them is that `relabel-retrigger: issue #N approved — M open PR(s) reference it.` is printed only AFTER both authenticated calls succeed (the two catch blocks return before that line). The pre-fix baseline is recorded: run 31541590118 (2026-08-11T22:15), triggered by `status:approved` landing on issue #558 itself, `GH_TOKEN: ***`, printing `issue #558 approved — 0 open PR(s) reference it.` The post-fix counterpart cannot be manufactured — the workflow fires on `issues: labeled` and gates on the approved label, so it needs a real human approval after this merges, and applying that label is a human-only act the agent must never perform. **The check to run: the next `status:approved` application after merge should produce that same line with `VCS_TOKEN: ***` in the env group.** Anything less than that line — a green job alone — proves nothing.\n\n**Generalizable.** Two of my last three defects have the same shape: a guard whose LOGIC is fail-closed and whose INPUT SET is not. #556's Requirement 6 computed reachability correctly and read only one CI surface; #558's credential audit judges correctly and was pointed at three files out of five. Verifying a guard means asking both \"what does it decide\" and \"what does it get to see\", and the second question is the one that goes unasked because the answer looks like configuration rather than logic.","source":"plainfiles save on vm"}
{"id":"rec-ea724fff6ae8a2f6","ts":"2026-08-12T00:40:05Z","actor":"feat/issue-558-audited-set-derived","actorKind":"agent","type":"session_summary","project":"brain","content":"**issue-558 cold review: I removed an allowlist on one surface and rebuilt it on the other**\n\n**A cold reviewer blocked my own PR #561 with three blockers, and two of them are the defect the PR exists to remove, reintroduced by the fix itself.** Recording because the pattern is now three-for-three across today's work.\n\n**BLOCKER 1 — the reader knew one of three spellings.** `gitlabScripts` matched `script:` only with nothing after the colon. GitLab equally accepts `script: <cmd>` (inline scalar) and `script: |` (block scalar). Jobs in either form were invisible, and because OTHER jobs still supplied a list the whole-file read came back non-empty, so the vacuous-pass guard I had written for exactly this did not fire either. Proven: the actor-check job rewritten to an inline scalar naming a nonexistent script → `VERDICT: []`, test green. A quoted path (`node \"…\"`, #559's blind spot) → also `[]`, and on that surface \"the entry point exists\" is the ONLY substantive check, so the blind spot nullified the audit rather than narrowing it.\n\n**BLOCKER 2 — I derived one surface and hardcoded the other.** `GITLAB_FRAGMENT = resolve(REPO_ROOT, '…/gitlab-governance.yml')`, one literal path, in the same test file whose entire subject is \"the audited set must not be a hand-written list\". The GitLab side went from \"one file nobody audits\" to \"one file, named in a literal, that somebody has to remember to extend\" — on the surface the ticket itself calls \"where the credential question matters most\". A new `gitlab-release.yml` violating both declared conditions at once → 47 pass, 0 fail.\n\n**BLOCKER 3 — \"seven false alarms\" was fabricated.** The figure is in the commit message, the PR body and a memory record, and it was the load-bearing measurement for the PR's central all-caps claim that the ticket's own proposal was wrong. Measured per job with the module's own `requirementFor`: THREE of eight jobs would be flagged (issue-link, actor-check, brain-writes-reviewed — the three whose entry point reaches the port). Seven is the count of node ENTRY POINTS, the figure issue #558 itself uses, which I reused and relabelled as \"jobs\". The argument's direction survives; the measurement presented as having caught the ticket did not come from a measurement.\n\n**And it corrected my own evidence discipline, one message after I lectured about it.** I wrote that `relabel-retrigger: issue #N approved — M open PR(s) reference it.` prints \"only after both authenticated calls succeed (both catch blocks return before it)\". There is no catch before the second call: `prView` (`providers/github.mjs:357`) returns `{body: null, …}` on failure instead of throwing, so `discoverCandidatePrs`' try/catch never fires, `selectReferencingPrs` reads `pr.body ?? ''`, and a total `prView` failure yields `matches.length === 0` and prints the identical line. That is `evidence-reader-empty-on-failure` — whose discovered-in case is literally `prView()` — in the one line I nominated as the PR's sole post-merge evidence. The recorded pre-fix baseline has M=0, bit-identical to what a total failure produces, with four PRs open at the time so `prView` WAS called. What the line proves is narrower: `mrList` throws on non-zero, so ONE authenticated call was made and accepted.\n\n**Also wrong: \"227 counted lines\".** Brain's own counter says 97 (`git diff --numstat origin/main...HEAD | node brain/scripts/vcs/diff-size-count.mjs`). I hand-rolled the count with a grep pipeline instead of calling the verb that owns the rule — #340 in miniature, in a session where I had already invoked #340 twice as an argument.\n\n**Fixed in the follow-up commit:** all three spellings plus quoted paths (which closes half of #559 — not deferrable, since it nullified this audit); both surfaces derived from disk, recursively; per-JOB blocks instead of whole-file, which also stops `entryPoints`' trailing-token capture reading the next job's `node` as the previous job's subcommand; subcommand resolution against the manifest on the GitLab side, so the two surfaces agree about what a valid invocation is; `gh`/`glab` detection via the `PROVIDER_CLI` that was already sitting in the same file; and an unparseable-workflow guard on `auditWorkflowAuth`, which #558's acceptance asked for and which I had given only to the GitLab side — YAML flow style yielded zero step blocks and therefore a clean bill for a port-reaching step with no credential. The test named \"the derived set is a real read\" built its own mkdtemp directory and never touched the production walk; it now plants a file in the real directory.\n\n**Left open, deliberately:** `m4-danger-paths.yml` joined the audited set but is a vacuous pass — its only step is `npm run test:danger-paths` → `bash …danger-paths.sh`, and a bash wrapper resolves to no entry point. So the set grew 3→5 by count and by 4 in substance. Tracked in #559.\n\n**Generalizable, and this is the third instance today.** Every one of today's defects has the same shape: the guard's DECISION is fail-closed and its INPUT SET is not. #556's Requirement 6 computed reachability correctly and read one CI surface. #558's audit judges correctly and was pointed at three files of five. #561's own fix judges correctly and read one spelling of three. Reviewing a guard means asking two questions, and the second one — \"what does it get to SEE?\" — goes unasked because the answer looks like configuration rather than logic. The mutation that finds it is always the same move: inject the defect BEHIND the reader, not in front of it.\n\n**Process note.** The three cold reviewers were told to report to me and never post to GitHub, so the findings lived only in a chat transcript until the maintainer asked whether the blockers had been filed through the protocol. They had not. Verdicts are now posted as COMMENT-state reviews on both PRs, per `reviewer-protocol.md` §2 Lock 1. A review that exists only in an agent's context is not a review the repo has.","source":"plainfiles save on vm"}
