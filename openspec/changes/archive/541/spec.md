---
status: draft
issue: 541
---

# Spec

## REQ-541-1 — the ruling exists with its cost
A ruling on (a)/(b)/(c) MUST be recorded, with the chosen option's cost stated alongside it.

## REQ-541-2 — the absence is counted
An observation reaching `exportObservation` without a §4 provenance block MUST be counted in
`share`'s accounting.

## REQ-541-3 — counted, not refused
It MUST still be written. `share` MUST NOT fail on it while the emitter does not exist.

## REQ-541-4 — the count is real, not constant
A compliant observation MUST NOT increment it.

## REQ-541-5 — the count is surfaced
It MUST be printed, not merely returned.

## REQ-541-6 — the measurement is re-taken
The 0/278 note MUST be re-measured against the current store, not quoted.

## REQ-541-7 — the backfill stays downstream
#368's decision about the 2157 historical records MUST NOT be taken here.
