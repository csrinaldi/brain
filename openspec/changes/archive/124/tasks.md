---
issue: 124
phase: tasks
---

# Tasks — #124

```brain-slice-scope/1
{"slice": 1, "claims": ["R124-1", "R124-2", "R124-3"], "terminal_pr": "this PR -> main"}
```

- [x] T1. Union in `defaultReadDenyActors`; the refusal names the list (R124-1, D3).
- [x] T2. Tests: agent-applied approval fails; message names the key; human
      passes unchanged; unreadable actor still fails closed.
- [x] T3. Amendment 3 pin: an agent's COMMITS still do not re-arm, and the
      commit reader still reads `agentActors` alone (R124-2).
- [x] T4. Staged, full suite, PR, cold review to APPROVE.
