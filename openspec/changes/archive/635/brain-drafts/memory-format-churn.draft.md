# Amendment draft — `memory-format.md`, the low-churn bullet

**For**: `npm run brain:promote -- openspec/changes/issue-635-doctrine-catches-up-to-code/brain-drafts/memory-format-churn.draft.md`

> Drafted by agent, applied by the maintainer. `brain/core/**` is Tier 3 —
> prohibited **even if explicitly asked** — and `brain:promote` is the sanctioned
> path.

## Why — a gap the first pass left, found by checking the promoted result

The first two drafts of #635 corrected the byte-identical premise in both
artefacts and restated the churn discipline in **ADR-0017 only**. Reading the
promoted files back showed `memory-format.md` still carrying the dead literal:

> *"`memory:reindex` / `memory:share` **MUST NOT rewrite the whole index every
> run**"*

ADR-0017 now says the rule governs the **diff, not the write**, and explains why
(`rebuildIndex` has always written the whole file, so the literal was never
satisfied by any implementation). `memory-format.md` states the opposite letter.

That is the same class of contradiction #635 exists to close — a normative doc
disagreeing with the decision it specifies — reintroduced by correcting one
artefact and not the other. It is repaired here rather than left for the next
reader to discover, and rather than being quietly folded into the earlier drafts
after they were signed.

#635's scope named `memory-format.md:155-156` (the byte-identical claim) and did
not name this bullet, because before Act 2 of the ADR amendment the two artefacts
agreed. The Act created the divergence; this closes it.

The anchor below was verified to occur **exactly once** in the target, against
the already-amended file.

```brain-amendment/1
target: brain/core/methodology/memory-format.md
issue: 635
```

## Act 1 — the low-churn bullet states what the rule governs

```amend-find
- **Low-churn** — `memory:reindex` / `memory:share` **MUST NOT rewrite the whole index every
  run**. Entries are stable-ordered by `id`; a reindex adds/updates only entries for newly
  appended records and leaves every other entry byte-identical, so `git diff index.jsonl` is
  proportional to the new records, not to the store size. This is the direct lesson of the
  ADR-0002 manifest churn that rewrote the full file each `memory:share` and blocked a raw
  `git pull`.
```

```amend-replace
- **Low-churn** — `memory:reindex` / `memory:share` **MUST NOT produce whole-file churn in the
  index**. Entries are stable-ordered by `id`; a reindex adds/updates only entries for newly
  appended records and leaves every other entry byte-identical, so `git diff index.jsonl` is
  proportional to the new records, not to the store size. This is the direct lesson of the
  ADR-0002 manifest churn that rewrote the full file each `memory:share` and blocked a raw
  `git pull`.

  **The rule is about the DIFF, not the write.** `rebuildIndex` regenerates the whole file from
  `records/` rather than patching it, and always has, so a literal reading of "rewrite" was never
  satisfied by any implementation. What stays proportional is what `git diff` shows — and it
  does, because the regenerated bytes are identical wherever the records are. The proportionality
  holds while duplicate groups are *intra-file*: a duplicate spanning two month files moves the
  winning entry's `file` field and produces exactly the churn this rule forbids, on a `share`
  that appended nothing. See ADR-0017, Amendment 1 (#635).
```
