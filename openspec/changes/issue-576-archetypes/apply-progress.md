# Apply Progress: #576 — single PR, one batch

**Worktree**: /home/gandalf/IA/brain-issue-576 (off origin/main @ 4cde50e). Strict TDD.

- [x] 1.1 archetypes.mjs — Map (the 'constructor' prototype trap), labels as
      checked values, port-field redeclaration throws; the Adversary seed
      already named its archetype, the Map now resolves it.
- [x] 1.2 verifier-review.mjs + firstPartyInstance(name) — §2 locks by symbol
      (each verified to exist in the doctrine), zero protocol literals.
- [x] 1.3 project-role.mjs + compileAgentsMd(docs, {roles}) — same-bytes-twice,
      brain- namespace, model omitted, backward byte-identity proven by the
      EXISTING drift test; goldens committed under __fixtures__ (check-brain-nav
      reads bare .md fixtures as orphan docs — learned by red).
- [x] 1.4 The projected reviewer's lock claims are load-bearing:
      prReviewComment exists, NO port verb matches /approve/i.
- [x] 1.5 The binding debt retired — challenger.{agent, model} were reserved
      and NEVER READ (measured); adversary-challenger joins the shelf;
      resolveJudgment serves challengerRole; AXIS half untouched; the debt
      ledger in roles.contract.test.mjs closes (all three of the class).
- [x] 1.6 ADR-0023 drafted FROM what exists (brain-drafts/), D6 + D3 reasons
      inside, emission wiring and declared-vs-active explicitly NOT decided.
- [x] 1.7 Suite 4670/0, repo:check + nav clean, work-unit commits, memory.
- [x] 1.8 THE MAINTAINER: brain:promote the ADR on this branch + `decision`
      label on the PR.
