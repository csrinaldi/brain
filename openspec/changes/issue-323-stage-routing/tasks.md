# Tasks: #323 S2 — single PR

- [x] 1.1 RED: pin the #812 field config byte-for-byte FIRST (custom stage +
      transport resolves and passes the new check).
- [x] 1.2 RED+GREEN: T1 — undeclared-stage refusal + C1 value-shape pin.
- [x] 1.3 RED+GREEN: T2 — assertRoutedStage (declaring engine passes;
      platform refused citing D6/#833; non-declaring/disabled refused with
      the port's words; unrouted passes through).
- [x] 1.4 RED+GREEN: T3 — the evidence demand in assertRoutableStage +
      runStage's pass-through; C2 pin (no shared reader reads engine).
- [x] 1.5 Slice ticket for the PR to close (the #816 pattern); full suite,
      gates, memory record, work-unit commits.
- [ ] 1.6 Push, PR, CI, cold review — authorized 02/09.

## Review Workload Forecast
~140 countable vs 1000 — single PR, no decisions pending.
