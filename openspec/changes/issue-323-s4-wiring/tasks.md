# Tasks: #323 S4 — single PR (slice ticket at PR time; #323 lives to S7)

- [x] 1.1 RED+GREEN: plain.runStage — the handoff (role resolved, target
      path from the accessor, steps; refuses an unbound lifecycle payload).
- [x] 1.2 RED+GREEN: gentle-ai.runStage — prompt FROM the port's
      instructions; spawn delegated to the claude transport (seam-injectable);
      refuses unbound evidence; the transport's own {ok, reason} rides through.
- [x] 1.3 RED+GREEN: C3 parity — same target path both engines; fixture dir
      passes the shared readers engine-blind.
- [ ] 1.4 Slice ticket, suite, gates, memory, commits, push, PR, CI, review.
